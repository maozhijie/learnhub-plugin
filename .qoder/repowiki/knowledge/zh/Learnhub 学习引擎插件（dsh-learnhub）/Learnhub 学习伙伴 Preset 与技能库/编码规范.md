- 每个 SKILL 以 YAML frontmatter 声明 `name` 与 `description`，正文用 Markdown 描述工作流、工具调用纪律与边界。
- 所有对 vault 数据（题库、FSRS 调度状态、图结构、state 流水）的变更一律通过 `learnhub_*` 工具完成，禁止直接编辑 YAML/JSON 文件。
- 图谱与题目变更遵循「提案 → 门禁校验 → apply」三段式流程，拒绝时根据报错修正并重交。
- 复杂任务通过 delegation 组委派给固定 persona 的子代理（`subagent_exercise_gen`、`subagent_reviewer`），主 agent 只协调不越权执行。
- agent.cordis.yml 按功能域以 `cordis:group` 分组（planning、compaction、delegation），并在 isolate 中启用 planMode/workflowEngine/compaction 等特性。