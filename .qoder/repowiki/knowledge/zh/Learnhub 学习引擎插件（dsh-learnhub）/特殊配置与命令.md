`npm run build`（即 `node build.mjs`）一次性完成 ui 构建、vendor 拷贝、lib/index.js、lib/engine.js、lib/client.js 编译、空白行清洗与 skills 同步到 `~/.dsh/skills`；`npm test` 先跑 `typecheck`（`arch-baseline` + `scan-ui-types`）再并行执行 `tests/*.test.ts`；`node scripts/link-peers.mjs` 补齐宿主 peer junction；本地调试用 `npx @deepseek-ai/dsh web` 启动宿主并加载本插件。