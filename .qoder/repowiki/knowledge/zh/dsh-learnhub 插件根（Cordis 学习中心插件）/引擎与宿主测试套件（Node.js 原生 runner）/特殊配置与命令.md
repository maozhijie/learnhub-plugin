`npm test` = `npm run typecheck` + `node --experimental-transform-types --test --test-concurrency=3 tests/*.test.ts`；单独跑某文件用 `node --experimental-transform-types --test tests/<file>.test.ts`；基线重录用 `tests/helpers/regen-fixtures.mts` 与 `regen-face-baseline.mts`；架构基线更新走 `node scripts/arch-baseline.mjs --update`；提示词变更过门 `npm run prompt-bump -- check|replay|compare`。