- ADR 采用 `NNNN-kebab-title.md` 命名并按数字顺序递增，每个文件记录单一架构裁决及其理由。
- 架构门与分层规则的阈值以精确匹配基线形式固化在测试中，基线更新必须与源码变更同提交且附 ADR 登记。
- 提示词模板变更需同步 bump 版本号并在 `PROMPT_CHANGELOG` 中登记 version/date/changeType/expectedDelta 四件套。
- 研究语料与评审结果以时间戳命名的 JSON/Markdown 快照归档，便于回放与回归比对。