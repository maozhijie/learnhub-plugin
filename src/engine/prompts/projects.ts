/**
 * 项目族与教练生长站的模型面散文（#237 / ADR-0075）：`project-decompile.ts`
 * 的修复轮死因标题、`coach-round.ts` 的双沙盘参考块、`growth-subsystem.ts` 教练回合的
 * 两块注入散文，全部收成**惰性字符串 + `{{变量}}` 占位符**，取值在调用点完成。
 *
 * 为什么（不是顺手整洁）：这些句子的措辞本身就是生产行为——修复轮标题决定模型「重出完整
 * YAML」还是「解释一下」，仲裁段的读法句锁死「模型推演，非承诺」（ADR-0025）。它们原先以
 * TS 模板字面量散在拼装函数里，散文与代码共用一套语法（散文里出现一个反引号或 `${` 就把
 * 代码改坏，AI 编辑尤其读不出「这里插的是语法还是字面量」），且「这次改了哪条提示词」无从
 * 对账。惰性串让散文对代码零语法风险，占位符表让变量面收窄成一处可对账的清单。
 *
 * 取值由 `prompt-render.ts::render` 在调用点完成（严格：缺变量抛、渲染后残留标识符形态的
 * `{{占位符}}` 也抛）。本文件的文本因此**只准**经 `render` 消费，不得直接进 `withContractLast`。
 *
 * 多行文本是**逐字节搬迁**：反引号内的缩进/空行就是提示词内容（仲裁段与回灌块带 YAML 与
 * 清单式排版），搬迁时一个字也不能动——渲染结果与搬迁前完全相同，零行为变化、不 bump 版本。
 */

/** 目标反编译修复轮死因标题（`project-decompile.ts::decompileRepairPrompt` 的 headline 参）：
 * 双产物（project + plan）同源，故校验门按整体拒绝回灌。 */
export const DECOMPILE_REPAIR_HEADLINE = '## 上一次输出未过双产物校验门（重新输出**完整** YAML 文档，修正下列全部问题；仍只输出一个 YAML，不要解释）'

/** 双沙盘仲裁参照块（`coach-round.ts::renderArbitrationEvidence`）：两份逐周分位带与新增
 * 清单由调用点算好当变量传入——本常量只持散文骨架（`addedClause` 是含/无新增两种措辞的
 * 取值，零新增时不造假差异来源）；末行换行是块尾约定。 */
export const ARBITRATION_EVIDENCE = `## 双沙盘推演（终审参照——模型推演，非承诺）

- 分歧声明（全量段）：{{disagreement}}
- 推演基准：每日约 {{minutesPerDay}} 分钟 × {{weeks}} 周；两份推演同一总体、同一随机种子序列（配对比较）。
- 计划一（现状照走）：{{beforeBand}}
- 计划二（含本批照走{{addedClause}}）：{{afterBand}}
- 读法：沙盘只模拟「记」的维持——本批的收益不在推演里，两带差异只读作本批的预算/保留代价参考；终审归你的教学判断。
`

/** 外部注入块（`growth-subsystem.ts` 轻量/全量段）：注入材料原样置中，前后两句是请求性质
 * 与算子纪律。**标题刻意中立**（ADR-0077 / #250）：同一 inject 通道既载里程碑计划修订
 * （#149）也载卡点自报（#248），按材料自带的标题读性质——旧标题写死「里程碑计划修订」，
 * 一条卡点自报套进去会被读成换线/补支请求，直接指向错误的算子纪律。材料自描述，块不预判。 */
export const COACH_INJECT_BLOCK = `## 外部注入（待裁决的请求材料）

{{inject}}

按材料自带的小标题判断它的性质（里程碑计划修订 / 卡点自报 / …），并遵守模板里对应章节的纪律。里程碑计划修订：换线 = 激活图上已有节点（内容生成/接入路线），补支 = 朝新里程碑长最小必要分支。任何注入都不豁免既有纪律——你的裁决仍走五算子与既定门，判断注入与就绪深度后照常产出（含零操作批）。`

/** 受理门反馈回灌块（`growth-subsystem.ts` 回灌重裁段）：拒绝原因与被拒裁决原文都原样
 * 携带（模型拿得到死因与修正起点），末句钉死「节点名/pre 必须逐字来自上方图面」。 */
export const COACH_GATE_FEEDBACK_BLOCK = `## 受理门反馈（上一版裁决未过受理门——被拒批次零落盘，图未改动）

{{feedback}}

上一版裁决原文：

\`\`\`yaml
{{previousYaml}}
\`\`\`

请对照拒绝原因逐条修正后，按模板重新产出完整裁决（course + note + route + ops）：节点名与 pre 引用必须逐字来自上方图面，概念必须已在登记表或本批 concepts 铸名。`
