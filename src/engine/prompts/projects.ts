/**
 * 项目族与教练生长站的模型面散文（#237 / ADR-0075）：`project-decompile.ts`
 * 的修复轮死因标题、`growth-subsystem.ts` 教练回合的外部注入散文（#273 起；#320 两站
 * 回单站后计划门反馈回灌随站退场）与**空图首级判据块**（#303 起），
 * 全部收成**惰性字符串 + `{{变量}}` 占位符**，取值在调用点完成。
 *
 * 为什么（不是顺手整洁）：这些句子的措辞本身就是生产行为——修复轮标题决定模型「重出完整
 * YAML」还是「解释一下」，仲裁段的读法句锁死「模型推演，非承诺」（ADR-0025）。它们原先以
 * TS 模板字面量散在拼装函数里，散文与代码共用一套语法（散文里出现一个反引号或 `${` 就把
 * 代码改坏，AI 编辑尤其读不出「这里插的是语法还是字面量」），且「这次改了哪条提示词」无从
 * 对账。惰性串让散文对代码零语法风险，占位符表让变量面收窄成一处可对账的清单。
 *
 * 取值由 `prompt-render.ts::render` 在调用点完成（严格：缺变量抛、渲染后残留标识符形态的
 * `{{占位符}}` 也抛）。本文件的文本因此**只准**经 `render` 消费，不得直接进 `withContractLast`。
 *
 * 多行文本是**逐字节搬迁**：反引号内的缩进/空行就是提示词内容（仲裁段与回灌块带 YAML 与
 * 清单式排版），搬迁时一个字也不能动——渲染结果与搬迁前完全相同，零行为变化、不 bump 版本。
 */

/** 目标反编译修复轮死因标题（`project-decompile.ts::decompileRepairPrompt` 的 headline 参）：
 * 双产物（project + plan）同源，故校验门按整体拒绝回灌。 */
export const DECOMPILE_REPAIR_HEADLINE = '## 上一次输出未过双产物校验门（重新输出**完整** YAML 文档，修正下列全部问题；仍只输出一个 YAML，不要解释）'

/** 外部注入块（`growth-subsystem.ts::coachDraft` 单站回路）：注入材料原样置中，前后两句是请求性质
 * 与算子纪律。**标题刻意中立**（ADR-0077 / #250）：同一 inject 通道既载里程碑计划修订
 * （#149）也载卡点自报（#248），按材料自带的标题读性质——旧标题写死「里程碑计划修订」，
 * 一条卡点自报套进去会被读成换线/补支请求，直接指向错误的算子纪律。材料自描述，块不预判。 */
export const COACH_INJECT_BLOCK = `## 外部注入（待裁决的请求材料）

{{inject}}

按材料自带的小标题判断它的性质（里程碑计划修订 / 卡点自报 / …），并遵守模板里对应章节的纪律。里程碑计划修订：换线 = 激活图上已有节点（内容生成/接入路线），补支 = 朝新里程碑长最小必要分支。任何注入都不豁免既有纪律——你的裁决仍走算子集与既定门，判断注入与就绪深度后照常产出（含停摆）。`

/** 空图首级判据块（`growth-subsystem.ts::coachContextPack` 注入；#303 / ADR-0092）。
 *
 * **起点资格四条迁自 ADR-0040**（单一行为单元 / 零复合概念 / 常识可起步 / 宁简勿繁）：
 * 它随种子链整体退役（#256 / ADR-0081）后未迁家，而空图首级的裁决权同期归了教练回合
 * （#273）——于是「新课后第一次生长」在无指引下临场发挥。迁家形态是
 * **材料注入**而不是新开第三族模板：模板族编码的是站的身份与输出契约，情境纪律进模板
 * 必然随情境枚举膨胀；而触发条件（前沿为空）本是引擎一步可判的事实。
 *
 * **四条原样照抄不足以定方向（#310 / ADR-0092 §修订）**：四条全给**上界**（多细 / 不
 * 复合 / 不需前置 / 越简越好），可被域外解同时满足——2026-09-16「数学基础」空课首裁
 * 实测铺出「拆日常事的先后步骤 / 说清输入输出 / 按简单规则手工跑一遍」：判据全过、朝
 * 声明终点的推进为零，而「宁简勿繁」的绝对化表述还反向激励往简猜。故补两条把台阶绑回
 * 既有输入（课程名 + 终点锚——ADR-0033「视角由目标携带」；系统无「学科」这种东西），
 * 并把「宁简勿繁」降为**同向候选之间的选择偏好**；原「Python 基础语法 / 装好环境并运行
 * 第一行代码」域外正反例删除，改为判据式说明（不收具体领域 → 模型无可照抄的范例）。
 * 四条本体不重新立法，只是从「起点资格」升格为「都朝终点推进的候选之间的选型标尺」。
 *
 * 触发口径是**前沿为空**（排除终点锚节点后未开始节点数为零；「仅终点」是其特例，删空
 * 普通节点后同样命中），不是「节点数 ≤1」的字面判断。`{{course}}` / `{{endpoints}}` 两个
 * 占位符由调用点取包内既有材料（课程名、终点锚名，不新增输入面），渲染后判据与方向在同
 * 一段里闭合、不给模型从课名反推的余地；仍一律经 `render` 消费以守本文件「文本只准经
 * render」的纪律。 */
export const COACH_FIRST_RUNG_CRITERIA = `- 首级判据（本回合**前沿为空**——这次裁决铺的是坡道的第一级台阶）：
  - 本课程：{{course}}｜声明终点：{{endpoints}}。第一级台阶必须读得出**朝这些终点之一推进了一步**——它是这条坡道的第一级，不是一段通用技能练习。
  - 朝终点可辨认（**先决条件**；不满足就换一个候选，其余判据不救）：台阶 = 动作句 + **该终点方向上的对象**。「把一件日常事拆成先后步骤」「说清每一步的输入与输出」「对给定的简单规则按步骤手工跑一遍」这类任何课程都能套的通用技能句不合格——它们与本课程的声明终点没有可辨认的推进关系。
  - 本课程词汇：台阶名里的对象取自上面的课程名与终点锚，不引入这两样之外的领域词；同时动作本身仍要零基础学习者从常识直接可起步（用本课程的词汇 ≠ 引入尚未教的术语）。
  - 选型标尺（**只在都朝终点推进的候选之间**比较——用来挑一个，不用来放宽上面两条）：① 单一行为单元——一次可完成的学习行为，不是一串待学的内容；② 零复合概念——名字连缀两个可独立教学的对象（与/和/及/的关系），或要先解释名字本身才能开学的行话泛称，都不合格；③ 常识可直接起步——零基础学习者不依赖任何尚未教的概念就能开始。
  - 宁简勿繁：**同向候选之间取更简的那个**——简是选择偏好，不是朝终点推进的替代品；反过来，台阶过大也会压断坡道第一级，上面两条上界（单一行为单元 / 零复合概念）就是防它的。
  - 操作化反例警示：首级被判据全过、方向为零的通用技能句占住（本轮实测就是「拆步骤 / 说清输入输出 / 按规则执行」三连），坡道第一级台阶就是断的——那些台阶服务不了任何声明终点。这是实测疼点，不是理论风险。`
