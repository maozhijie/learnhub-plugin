# Learnhub

DeepSeek Harness 的学习引擎插件：题目级 FSRS 刷卡调度 + 课程图谱 + XP 时间账本，事实源在 Obsidian Vault。

## Language

**Node（节点）**:
一次独立的学习行为单元，即一节完整的课。非原子知识点——除非该概念自身极其复杂。容量动态：由图谱生成时分配 est，上限 30 分钟，极端复杂可扩展至 60 分钟；est 本身可取任意值。
_Avoid_: 知识点、课程、课时

**Section（节段）**:
节点内部的一个内容区块，由大纲阶段规划、逐个生成与质检；一个节点由若干节段组成。节段不可再分层。其数量、长度与类型配比应随节点复杂度弹性伸缩，而非固定。
_Avoid_: 小节、章节、模块、区块（易与 Block 混淆）

**Complexity Tier（复杂度档位）**:
节点按难度、认知层级（bloom）与前置规模折叠出的内容规模级别，共三档（低/中/高）。内容生成据此推导节段数区间、篇幅预算与每节段题量；est 只作容量上界，不参与分档。面向内容生成与审计，学习者的感知是课程长度自然随节点难度变化。
_Avoid_: 档位（单独使用易与模型思考档混淆）、难度档、篇幅档

**Question（题目）**:
题库中的单题，自带一张 FSRS 调度卡，作答对错直接推进该卡。当前固定从属于一个节点，不跨节点复用。
_Avoid_: 试题、考题、练习

**学习日（Learning Day）**:
一场学习归属的「日」：到期判定、同日守门、XP/streak/真实保留率等按日聚合、「今日」视图，全部以学习日为单位；凌晨落在日界之前的时间归属前一个学习日。出处戳（生成/训练日期、备份命名、运行日志）永远用日历日，不参与学习口径。
_Avoid_: 今天、当日（单独使用易混日历日）、自然日

**日界（Day Cutoff）**:
一个学习日结束、下一个学习日开始的本地时刻：vault 运行时配置，默认凌晨 02:00（熬夜过了午夜仍算前一天）；设为 00:00 即退化为日历日口径。
_Avoid_: 截止时间、跨日时刻、午夜

**Advance（推进）**:
对一张调度卡的单次真实推进：先过「一卡一学习日至多一次推进」门（当日已真实推进 = 作答统计的 last 命中当前学习日；合成初始化写调度锚点但不算推进、不占当日额度），再按学习日重算 FSRS 并合并作答统计。推进机械全卡域同一（题库卡、笔记源卡、我的卡、Anki 回填事件）；各作答入口只决定评分语义（自动判定 / 复习自评档 / 合成初始化）与账本组合。Anki 回填按事件的学习日推进，且 vault 当前学习日已有任何调度动作（含合成初始化）时只留档不再推进（回放通道从属）。
_Avoid_: 刷新调度、更新卡片、打卡

**Review Queue（复习队列）**:
到期调度卡构成的跨课程扁平队列：全部启用课程中 due 不晚于当前学习日的题卡与我的卡（笔记源镜像卡以伪课程并入、我的卡按 ADR-0021 汇入），按预测遗忘风险升序为主、同档难度渐进出示；支持按课程/节点过滤，作为定向复习的直达入口（A3 软闸与成分技能回退建议项消费）。不含从未调度过的新题——例外是我的卡：它没有别的首推入口，未调度自注卡直接在队尾。一卡一学习日只推进一次调度。
_Avoid_: 复习列表、任务包、到期清单

**Forgot（忘记）**:
学习者申报「想不起来」：不作答直接翻面看答案，调度与统计均按答错记，不奖励 XP。受 5 秒主动回忆门控——卡面展示满 5 秒才允许申报，防止以申报代替回忆。
_Avoid_: 不会、放弃、跳过

**Mastery（掌握度）**:
对节点当前掌握状态的连续度量（0–1），由记忆稳定度（FSRS）与练习证据复合派生，无自评；防饱和——一次全对不会到顶，随复习推进渐近满分。它是读侧派生的视图，不落盘。目标始终是忠实表达学习者对该节点的实际掌握程度。
_Avoid_: 熟练度、分数、进度、正确率

**Answer Accuracy（作答正确率）**:
节点题库的累计答对比例（答对次数 / 作答次数）。只用于完成门禁与单题作答统计；不是掌握度，任何界面不得把它当掌握度展示。
_Avoid_: 正确率（单独使用易与 Mastery 混）、掌握度

**True Retention（真实保留率）**:
到期复习中实际答对的比例（Pass/Fail 口径：每卡每学习日只取第一次推进，答错/Forgot 记失败，答对含自评通过记成功）。只读侧诚实度度量：与 FSRS 预测保留率对照，衡量「调度预测与实际一致」；不参与完成门禁、不是 Mastery、不进任何账。合成首复习不是真实作答，不计入。
_Avoid_: 正确率（易与作答正确率混）、保留率（单独使用易与 FSRS 预测保留率混）、掌握度

**Learner Output（学习者产出）**:
学习者主动产出的学习动作与产物（自注解释、自造卡、讲解稿、预测、目标偏好），是过程资产与复习对象：带自己的调度与档案，AI 对照该节点已教要点给对错反馈。除 Learner Card 按 ADR-0021 汇入复习队列、复习走 Unbound XP 外，产出永不进入 Mastery、作答正确率、完成门禁、XP 账本或任何 canonical 调度卡——是学习机制，不是测量证据。
_Avoid_: 作业、作品、产出（单独使用易与内容管线产物混）

**XP**:
时间账本，记录有效专注的量（1 XP ≈ 1 分钟）。本质是记录系统；激励与调度反馈只是衍生需求，不是核心。入账判据=原生专注时长是否可信，不属于哪个区：节点学习时间与执行事件入账，回执（时长不可证）与习惯重复（无时长语义）不入。账目分绑定实体与无绑定两种：前者归课程/节点/技能条目报表，后者（我的卡、笔记源复习）只入总账、每日目标与 streak，出处留待事后归账。streak 是账本的派生指标（行为流水即事实，零新增数据），按学习日聚合、宽容口径（连续漏天 ≤1 天不断链，漏天不归零可恢复；与习惯的宽容 streak 各算各的）。
_Avoid_: 积分、经验值、金币

**Unbound XP（无绑定 XP）**:
不绑定任何实体的 XP 账目（我的卡与笔记源复习的入账形态，ADR-0021）：行落中心级账本、detail 带产出出处（卡 id/源 id），计入总账、每日目标与 streak，不进逐课程/逐节点报表；出处足以支持事后归账——归账是视图操作，不是数据迁移。
_Avoid_: 游离XP、公共池、无主XP

**Course（课程）**:
Vault 中一个待学的完整学科，拥有自己的图结构、题库与调度状态。
_Avoid_: 学科、专题

**Note Source（笔记源）**:
注册为复习对象的 vault 个人笔记文件（任意中心外文件夹/文件，文件夹 = 批量入口；学习中心与用户排除清单恒不在册——排除清单只管未来注册，摘除已注册源走解除注册）。引擎只读它：从正文出题、把题库与 per-question FSRS 落在学习中心镜像区，永不写入该笔记文件。笔记删除/改名 = 源链接 Missing（合法空态、卡池挂起），正文被编辑 = 内容漂移（提示可重出/归档）；用户笔记不属于引擎契约，永不判 Broken。
_Avoid_: 知识库、笔记课程、文档源、排除名单（用户排除清单是注册纪律，不是独立的笔记源状态）

**Anki Mirror（Anki 镜象）**:
推送到桌面 Anki（经 AnkiConnect）的到期卡副本（C2）：Anki 是纯作答通道，跨界的只有原始作答事件（Again/Hard/Good/Easy → 答错/复习自评档），调度永远由 vault 的 ts-fsrs 重算，Anki 侧排期输出一律不作数。每次导出按 vault 到期集校准/重建镜象（含已归档/已重生成/已消费卡的移除）；镜象是可丢弃的派生状态，与 vault 不一致时以 vault 为准，永不判 Broken。一题一学习日只推进一次在导入时守住。
_Avoid_: 双调度器、Anki 同步（与官方同步混）、双向同步

**Region / Block**:
为提升图生成质量而设的层级分组。概念上希望深度可变，工程当前固定两级；其存在服务于生成，不是学习者感知的概念。
_Avoid_: 章、节

**Settle（对账）**:
节点完成时锁定其 XP 的动作。按节点定价（est × 难度校准）锁定，实际耗时不直接入账。
_Avoid_: 结算、提交

**Stage（节点状态）**:
节点的离散流程状态（unseen/ready/learning/review/mastered/skipped）：记录学习者走到哪一步、该卡当前该如何处理（FSRS due 驱动）。与 mastery 各自独立演化，仅在个别点交叉；由阈值派生是可选的未来方向。
_Avoid_: 进度、阶段

**Missing（缺失）**:
预期学习者数据对象没有对应文件或记录，是合法的空状态。
_Avoid_: 空、损坏

**Broken（损坏）**:
预期学习者数据对象存在，但无法解析或未通过数据契约；不能被读成空值或默认状态。
_Avoid_: 坏档、异常数据

**Data Check（数据体检）**:
只读盘点学习者数据完整性的动作：报告 Missing 与 Broken，不修复、不写入。
_Avoid_: 修复、清理、健康分

**Health Score（健康分）**:
图谱结构质量的度量（0–100）：命名规范、est 覆盖、前置完备、收敛度、结构卫生。面向图的作者与生成器；教学质量度量是未来方向。
_Avoid_: 评分、质量分

**Scale Floor（规模底线）**:
scope 分析时宣布的课程目标节点数区间，图谱交付的绝对规模门槛。
_Avoid_: 节点配额、最低节点数

**Proposal（提案）**:
待应用的图谱变更集合，apply 后落盘并附快照。工作流惯例而非硬性设计约束，可随时调整。
_Avoid_: 请求、审批

**Pre（前置）**:
节点间的时序依赖边：学本节点之前应先掌握的节点。
_Avoid_: 依赖、前置条件

**Jump（认知跨步）**:
一条 pre 边的质量缺陷：前置到节点的认知距离超过一次可学习单元，中间缺应有的过渡台阶。图结构可标出候选，判定归属图的作者。
_Avoid_: 跨越度、难度跳跃、断层

**Float（空降节点）**:
课程位置靠后（按区/块顺序）却没有任何 pre 的节点：内容凭空拔高的结构信号。
_Avoid_: 孤立节点、孤儿节点

**Enc（成分技能）**:
本节点练习真实调用的前置技能边（带权重），必须位于本节点的 pre 传递闭包内。同时服务内容生成（练习必须真实调用）与图审计，也是调度（复习回退路由）的消费边。
_Avoid_: 子技能、组成

**Practice Node（实践节点）**:
type 为 practice 的交互模拟交付节点，不出题库练习题，靠交互件成绩积累练习证据（同节点每学习日限一次）。
_Avoid_: 实验、作业

**Practice Evidence（练习证据）**:
节点作答与交互成绩的统一流水及 EMA，mastery 的辅助来源（约 30%）；题目作答与交互件结算都汇入此通道。
_Avoid_: 练习记录、作业记录

**Partial（部分完成）**:
生成管线已产出部分有效产物，但未完成全部必需阶段的终态；不是 done。
_Avoid_: 成功、带错完成

**Generated（已生成）**:
节点存在至少一节 ready 正文，点开即有内容可读；列表与图上三态标识之一（未生成/生成中/已生成），数据源是节清单而非管线终态。
_Avoid_: 有内容、已完成、done

**Generation Queue（生成队列）**:
全局串行的后台生成任务队列：任意入口入队即返回，FIFO 按序执行，同一时刻只跑一个节点管线；重启后暂停待手动恢复，学习/复习不受其阻塞。
_Avoid_: 任务列表、并发生成、后台链

**定向补生成（Targeted Reinforcement）**:
练习会话卡节时学习者触发的补题动作：只为当前未过关的节补少量同类题，产物强制归属该节、无法归类的题拒收不入库；每次少量，节点题库设软上限（到达须显式确认）。新题按节追加进当前会话轮次（ADR-0027）。与校准重出区分——那是难度失衡建议驱动的整节点动作；与重生成区分——那是归档旧题另出新题、新卡从零调度（ADR-0028）；两者都不是定向补题。
_Avoid_: 再出题（UI 按钮口语）、补题（口语泛称）

**Today Pin（今天学它）**:
学习者把某节点 pin 成当日推荐榜首的目标动作（Learner Output）：只改推荐读侧排序（课程内置顶、跨课程仍按全局排序语义），保留就绪提示、不设门禁；仅作用当前学习日，日界过后自动失效。引擎给榜首配一句「为什么是这节」的自然理由——提议非指令。
_Avoid_: 置顶收藏、计划任务、学习目标（易与周目标混）

**JOL Prediction（预测）**:
复习流翻面前的一档三点自我预测（会/不会/没把握）：按抽查率（默认约 1/3、可全局关）出现在有信息价值的卡上（将忘未忘、难度中段、曾有偏差），单点即过、可忽略。配对沉淀为自评校准画像的第一个源（见 Self-Calibration）；只展示给学习者——不进 Mastery、XP 或任何 canonical 度量。
_Avoid_: 自评（易与作答后自评难度混）、打分

**Difficulty Band（难度带）**:
练习/复习会话开始时学习者可选的难度偏好（简单/标准/挑战）：作为 A1 自动选题的带权偏好（挑战抬高当次目标带、简单放宽），不是过滤通道；答错/忘记的防挫回落照常生效。默认标准 = 纯 A1 自动。「可用的困难」教练监视长期选择分布给只读提醒，无门禁。
_Avoid_: 难度过滤、难度模式、关卡

**Challenge Point（挑战点）**:
功能任务难度 × 学习者技能交出的个体化最优练习点（Guadagnoli & Lee 2004）：因人而异、随水平移动，没有对所有人最优的练习计划。引擎在跨区观测面聚合各区已有读侧信号估计它，但恒温器不是自动控制器——只出只读建议，任何参数调整走既有入口、逐条显式确认（ADR-0024）。
_Avoid_: 恒温器（那是特性名，机制是提议面）、最佳难度、心流通道

**Learner Card（我的卡）**:
学习者产出卡的独立域（E1）：三种卡面（提示重述/挖空重述/自注讲解），每卡自带调度块（默认 FSRS 参数），进复习队列走复习自评语义，一卡一学习日一次推进；复习入账走 Unbound XP（ADR-0021）。不进题库九题型、不进 Mastery/作答正确率/完成门禁/任何节点调度面。「讲给我听」的存档目标。
_Avoid_: 自建题（与既有自建题入口混）、学习卡、错题本

**Explain Back（讲给我听）**:
学完节点后的费曼讲解动作（E2）：学习者用自己的话讲，AI 扮完全不懂的初学者只按正文要点追问（抓含糊/跳跃/说错，不评分不超纲），收尾给定位反馈（是非 + 定位 + 可怎么补），可存为 Learner Card。判词只入 E 档案，零 XP、不写 canonical；入口自愿可关。
_Avoid_: 讲解（与错误当下的「讲解这道题」混）、教学相长、复述

**Project（项目）**:
Course 的姊妹实体：学习者真实在做的实践（有界项目区的载体），以周/月计，撑不起节点的容量契约，故不是节点。生命周期 active/paused/delivered/archived：终态可选——无截止项目合法地永驻 active、无催办义务；且无不可逆转移，任意状态可重开回 active。不进题目门禁、无 mastery、无 FSRS 卡、不持 enc 边；持自己的执行事件流（喂档位移动与诊断），执行证据沿被行使的 enc 边向节点练习证据通道单向回流；只按里程碑对账，账本永不强制关闭。
_Avoid_: 实践节点（与 Practice Node 混）、任务、作业、项目课

**Milestone（里程碑）**:
项目内的有序交付检查点：里程碑计划可修订（走 Proposal 快照语义，非生成时冻结）；是里程碑级对账与检索点的载体。无 FSRS 卡、无 mastery，不是节点。
_Avoid_: 阶段（与节点 Stage 混）、检查点、目标

**Fading Tier（渐退档）**:
项目对学习者的支持强度档位（骨架→补全→独立）：生成产物形态随档位变化，档位移动判据=档内表现——引擎提议、学习者可改；与 mastery、题目门禁无涉（mastery 对项目只做入档推荐）。
_Avoid_: 支持级别、脚手架、难度档（与 Difficulty Band 混）

**Receipt（回执）**:
学习者提交的外部练习证据：自报即可信（账本是自己的，谎报自损，系统不设反作弊门），形态自由——文字描述、图片、软件导出、教练签核皆可，材料只为降低维护与评审成本，不是可信性门槛。经 AI 量表评审产生带来源枚举的练习证据事件、同权进 EMA；永不判 Broken、永不进内容管线、不是复习对象；零 XP、不推进任何 FSRS 卡。v1 挂实践节点，机制按通用实践主体建模（项目/技能条目载体后续接入）。
_Avoid_: 作业提交、打卡、证明材料

**Habit（习惯）**:
学习者为重复行为立的一等对象（无界实践区载体）：一条执行意图（线索-行动规格）+ 自动化进度曲线（自评、渐近，自变量=重复次数，中断不衰减）+ 宽容 streak（漏天无损）。无 FSRS 卡、无 mastery、无到期语义——调度者=情境与日历，引擎不推送不提醒；重复由学习者自报，无门禁、不防作弊（自测不是考试）。曲线与 streak 只展示给学习者，永不进 Mastery、练习证据、XP 或任何 canonical 度量；归档可逆，无截止习惯合法地永驻 active。
_Avoid_: 打卡（暗示防作弊门禁）、习惯卡、日常任务、提醒、自律目标

**Execution Intention（执行意图）**:
「在【时间/地点锚】之后做【单一具体行动】」格式的计划记录：习惯持有一条作为线索-行动规格，目标偏好也可挂载（只覆盖推荐读侧）。格式锁死为稳定线索+单一行动，多行为链、模糊线索掉出证据范围；共享类型、各归其主——习惯的意图持久（实体身份的一部分），目标偏好的意图随其读侧语义走（可过期）。
_Avoid_: 计划（单独使用）、目标（与今天学它混）、待办、提醒

**Skill Entry（技能条目）**:
无界实践区可排期的实践主体（乐器、运动、编程等持续技能），执行事件调度 lane 的载体：lane 与题目 FSRS 并行，不复用题目卡、不进复习队列；维持节拍上限保证久置技能低频复活。无 mastery、不进题目门禁；回执可挂它作证据（永不推 lane）；项目执行事件不传播给它（项目↔技能条目关联留后续票）。
_Avoid_: 技能卡（持的是调度 lane 不是题目卡）、实践节点（Practice Node 是有界区交付节点）、习惯（Habit 是无到期自报域）

**Execution Event（执行事件）**:
一次真实执行加表现评级（1-4 整数+来源枚举）构成的调度事件，非对错判据（区别于题目作答）：推技能条目的调度 lane，独立成流。评级映射归各生产者，推荐默认=可观测执行证据的确定性映射，分数评级禁止直喂。按其原生专注时长入 XP 账本、进 streak 口径（时长可信性判据，ADR-0019）；与回执（证据语义，零 XP、永不推 lane）、习惯重复（无时长语义、无事件语义）三分；一次实践活动可同时产生执行事件与回执，各记各的，事件可引用回执材料作附件。
_Avoid_: 练习记录、打卡、回执（与 Receipt 混）、执行意图（与 Execution Intention 混）、习惯重复

**Self-Calibration（自评校准）**:
跨源「学习者自评档 × 同事件客观结果」的配对画像：分源自省面，不宣称跨域人格特质（域特异成分显著，全局聚合只作参考视图）。配对契约=凡同一事件同时携带自评档与客观判分即落一条配对（源：JOL 预测×作答即日、执行事件自评×判分随 #89、回执自报×AI 评审候选随 #88）。只展示给学习者 + 显式呈现层提示（可关）；永不折扣学习者自评对 canonical 的驱动（复习自评原样推 FSRS）、不进 Mastery/XP。FSRS r_pred 自校准是模型体检（主体=引擎），不属画像域。
_Avoid_: 校准曲线（单源旧称，画像含其超集）、人格画像、自评（与作答后自评难度混）

**Problem-First（先做后教）**:
高难节点的生成管线顺序变体（PS-I，#81）：bloom 高阶层或 difficulty ≥ 4 的节点，大纲第一节为挑战节（例题型、只给题面不给解法），讲解围绕挑战题展开并在收尾回扣解答——学习者带着尝试缺口听课。与样例效应按节点难度分流：中低难节点照旧先教后练，非全局反转；只改生成顺序，完成门禁与调度语义零改动。
_Avoid_: 反转教学（暗示全局反转）、挑战优先（非词条名）

**N-of-1 实验（N-of-1 Experiment）**:
在单一学习者身上把同一批合格对象（卡/节点/会话）随机分臂、用其真实学习结果测干预效应的自我实验（ADR-0023）：实验变量只允许是引擎可控的内容/课程设计参数（PS-I 顺序、检索点位置与密度、CI 编排、难度带默认、会话组成），调度核心永不作实验变量；主结局预登记（调度侧=真实保留率、练习侧=EMA），臂标注进复习日志；零 XP、不进 Mastery；启动走提案-确认制。
_Avoid_: A/B 测试（人群间随机化）、自我实验（口语泛称）、实验引擎（系统组件名）

**沙盘（Sandbox）**:
用现有 FSRS+mastery 模型对学习计划做蒙特卡洛推演的只读视图（ADR-0025）：输入每日分钟目标×时长与意向课程/节点集合，输出推演终点的掌握度地图与分位带。输出是分布不是承诺（「模型推演，非承诺」措辞锁死），支持计划谈判；零写侧——不进门禁、不进调度、不改账本；模型与调度同源，不引入独立记忆模型。
_Avoid_: 预测沙盘（「预测」已属 JOL 预测与 FSRS 预测保留率）、模拟/推演（单独使用）、可行性判定（沙盘不给判定）

**周复盘（Weekly Kata）**:
全局每周一张的结构化复盘（五问：目标条件/现状/障碍/下一实验/预期所学，ADR-0026）：现状由引擎用上一学习周的真实数据自动填，其余四问学习者作答；有界（项目/课程）与无界（习惯/技能条目）在同一张里相遇，是两区铰链。学习周=日历周按学习日折叠。归 Learner Output 域：零 XP、不进 Mastery、本身不做 FSRS 卡；无推送，入口常驻、缺勤不罚；「下一实验」可一键转 N-of-1 实验提案或执行意图。
_Avoid_: Kata（单独使用）、周报、周总结、复盘（单独使用=泛 debrief 概念）


**目标反编译（Goal Decompilation）**:
从「目标项目描述 + Vault 笔记」反推学习资产的逆向设计动作（4C/ID 任务分析 + PjBL：知识为项目服务），输入一次产出**双提案**——里程碑计划草案（project_plan 提案，与计划修订同一 apply/reject 通道）与知识子图（图谱域单 pending 提案：显式目标课程则追加，无课程则独立成新课程骨架）——都走人审（Proposal 通道）；检索面复用 Vault 先验（只读，摘录注入反编译上下文）；apply 前零 canonical 写入（不动课程图、不写项目.md 的 plan），关联永不构成门禁（ADR-0015 裁决 6）。
_Avoid_: 反向工程（口语泛称）、自动建课（只产提案不落盘）、目标拆解（拆出的是知识点清单，反编译拆的是可交付里程碑 + 知识子图）

**Project Exec Stream（项目执行事件流）**:
项目自有的追加式执行事件流水（projects/<id>/exec.jsonl；事件=真实执行+表现评级 1-4+来源枚举+行使节点，多记一档位快照供「档内表现」归属）：ADR-0015 裁决 4 的落地，与节点练习证据通道是两条流。上行——沿被行使的既有 enc 边（两端都在事件行使节点内）把表现分单向复制进两端节点的练习证据 EMA（复用 applyPracticeEvidence，零新存储形态；行使判定机制精细化归 P-6）；下行——喂渐退档入档推荐与掌握交叉。零 XP、零 journal、零 FSRS/sessions/srs（项目域唯一 journal 例外仍是 milestone_settle）。
_Avoid_: 练习记录（节点练习证据通道是另一条流）、回执（Receipt 是证据提交非执行）、执行事件（泛称；技能域那条推调度 lane，两域各自成流）

**Mastery Cross（掌握交叉）**:
项目域的核心诊断视图（2×2，P-7）：X 轴=关联节点的 Mastery 均值（陈述性掌握），Y 轴=项目执行证据（项目执行事件分 EMA，0.7/0.3），两轴同阈 0.6（params 集中）；四象限——会而不会用（高掌握×低执行）/ 会用而不牢（低掌握×高执行）/ 健康（高×高）/ 补底（低×低），证据缺失按该轴低侧落位。上行回流后 mastery 变化在这里落位显示；只读、零门禁——它诊断，不判决。
_Avoid_: 四象限图、矩阵诊断、掌握度矩阵

**练习会话（Practice Session）**:
一次进入练习流、从当前节推进到走完或中途离开的连续作答过程：当前题目与作答记录归属会话本身，不归属页面；「同一次学习里每题只作答一次」的边界就是会话。会话存续期间，后台生成与内容修订不重置它（ADR-0027）。
_Avoid_: 刷题、答题进度、学习记录

**直通卡（Revealed Question）**:
本学习日已推进过的题在练习会话中的展示形态：题面、最近一次作答对错、正确答案与解析直接可见，无作答控件、仅可前往下一题。对错中性——不计连对、不入作答统计与练习证据、零 XP；判定口径与推进的「一卡一学习日」一致。
_Avoid_: 重做题、答案模式、复习模式（与复习队列混）

**判卷逃生门（Grading Escape）**:
AI 判卷多次重试仍失败时对单题的逃生动作：本次作答整事务放弃（零落盘、不推 FSRS、不入统计与连对），该题在本会话按直通卡口径翻面、会话继续推进。只由引擎故障触发，不是学习者作答语义——不计 Forgot、不产生对错记录，也不降低判卷标准（成功判卷仍走完整事务）。
_Avoid_: 弃答（学习者主动申报）、跳过本节（整节推进动作，与判卷故障无关）、跳过（单独使用）

**唯一答案填空（Unique-Answer Blank）**:
填空题的唯一合法形态：答案存在唯一文本写法（术语、名称、符号），任何学过的人写出的正确答案至多差空白与大小写；题面自身锁定唯一答案。数字不进填空（数值题以容差判卷承载），代数式不进填空（单选以正确式+典型错误干扰项承载）——文本比较判不出恒等变形，假阴性等于伪造学习证据（ADR-0029）。
_Avoid_: 简答题（那是开放题）、表达式填空、多空填空

**瑕疵题（Defective Question）**:
题目自身有缺陷的题：答案键/解析与题面矛盾、题意含糊，或考察正文之外的内容——与学习者正常答错的作答相对。判错不等于题错：错题是作答记录，瑕疵题是题目自身的缺陷，由申诉复核认定，处置走勘误（ADR-0031）。
_Avoid_: 错题（那是正常作答记录）、坏题（口语）、废题

**申诉（Dispute）**:
学习者对判错的异议动作，主张「题目错了而不是我错了」：提交后由 AI 两阶段复核（先只看题面独立解题、再与存储键/解析对账）三态裁定——键错/题面坏/题没问题。复核说题没问题仍可强制豁免，但豁免是「这题不算」、永不产生得分；改判只能来自键修改后的重判（ADR-0031）。
_Avoid_: 投诉、举报、改答案

**勘误（Erratum）**:
对瑕疵题的处置记录：改键/改解析（题面完好时）或作废归档重出（题面损坏时）。改键后用新键重判被申诉的那条作答，可改判为对并补记；只冲正被申诉条目，历史作答不追溯重判。
_Avoid_: 修改题目（泛称）、纠错

**冲正（Reversal）**:
对已落盘作答判罚的抵消凭证（state/勘误.jsonl 追加，只增）：原始作答流水永不改写，XP 与作答统计等聚合账读侧按净值算。FSRS 调度不冲正、review-log 不抹除——调度误差自愈，证据流水不为便利回滚（ADR-0004/0031）。
_Avoid_: 回滚、撤销、删除记录

