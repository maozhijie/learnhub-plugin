# 用户数据区分缺失与损坏，判卷失败不伪造分数

题库、注册表和课程笔记是学习状态的事实源，因此读侧必须区分 Missing 与 Broken：缺失是合法空状态；已存在但解析或校验失败的数据必须暴露，并阻断依赖它的操作，不能静默读成空题库、空课程或默认笔记状态。AI 判卷失败时，本次作答不得产生分数，也不得推进 FSRS、practice 证据或 mastery。兼容存量数据要靠显式盘点和修复，不能靠吞错降级。
