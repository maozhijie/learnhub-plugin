# 实践节点不更新 FSRS：mastery 上限 0.3 的工程妥协

实践节点（type: practice）没有题目级 FSRS 卡，交互模拟成绩只汇入练习证据通道（practice EMA，mastery 的 30% 权重），不推进任何 FSRS 数据；因此实践节点的 mastery 上限约为 0.3。这是有意的妥协：自动生成的交互模拟与 FSRS 调度数据找不到自然的对接点，强行绑定会污染调度语义。未来方向：将"完成一次交互"整体作为一次调度行为绑定 FSRS 数据；在此之前，不要"修复"这个上限——它是当前机制的自然结果，不是 bug。
