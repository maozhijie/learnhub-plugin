---
station: 题目生成
arm: control
variant: 重申契约
run: 8
tool_called: false
delivered: true
schema_ok: true
input_tokens: 196
output_tokens: 10731
duration_ms: 50591
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5242 字符，变体后缀=有）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

node: 变量是什么
questions:
  - id: q1
    kind: single_choice
    q: '同一笔余额在早上是 $50$ 元、晚上是 $20$ 元。关于“余额”这个变量，下列说法正确的是？'
    options: ['变量是给一个会变的值起的名字：名字稳定，值可以随时刻改变', '变量是一个固定不变的值，一旦确定就不会再改变', '变量就是值本身，名字和值是同一个东西', '每个变量只能有一个值，所以早上和晚上的两条记录中必有一条是错的']
    answer: A
    explanation: '变量是给一个会变的值起的名字：同一个名字“余额”在早上对应 $50$、晚上对应 $20$，两次说的是同一个变量。关键区分是名字稳定、值随时刻改变，所以 A 正确。最易错点：把变量当成某个固定不变的值，就会觉得两个时刻的记录互相矛盾。'
    difficulty: 1
    section: 概念：变量是什么
    invokes: 变量
    uses: [变量]
  - id: q2
    kind: true_false
    q: '判断：读取一个变量时，拿到的总是「写下它那一刻」的那个值，与此刻变量的值无关。'
    answer: 错
    explanation: '读取拿到的永远是此刻的值，所以这句话是错的。若一个名字先被写进 $50$、之后又被改成 $20$，此刻读取得到的是 $20$ 而不是 $50$。最易错点：把名字绑死在最早写下的那个值上，忘记值会随每次赋值而改变。'
    difficulty: 1
    section: 概念：赋值与读取
    invokes: 变量
    uses: [变量]
  - id: q3
    kind: matching
    q: '某账户按顺序出现了三条记录（第三条的打印发生在第一、二条之后）。请把每条记录与它的性质配对。'
    options: ['记录二：取出“余额”此刻的值减去 $30$，把结果赋回“余额”', '记录三：显示“余额”，屏幕上出现 $20$', '记录一：把 $50$ 赋给“余额”']
    answer: ['这是先读取再赋值：先读出此刻的 $50$，算出 $20$，再把 $20$ 写回同一个名字', '这是读取：用名字换回此刻的值 $20$，这一步不改变变量', '这是赋值：把 $50$ 写进名字“余额”']
    explanation: '记录一把 $50$ 写进名字，是赋值；记录二先换回此刻的 $50$、算出 $20$ 再写回，是先读取后赋值；记录三只是把此刻的值取出来显示，是读取，不改变变量。关键一步是 $$50 - 30 = 20$$ 所以记录二之后“余额”变成了 $20$。最易错点：把记录二当成纯赋值，忽略等号右边必须先读取当前值。'
    difficulty: 2
    section: 概念：赋值与读取
    invokes: 变量
    uses: [变量]
  - id: q4
    kind: single_choice
    q: '某库存程序按顺序做四件事：① 把 $120$ 赋给变量“库存”；② 取出“库存”的当前值减去 $45$，把结果赋回“库存”；③ 显示“库存”；④ 把 $20$ 赋给“库存”，再显示一次。两次显示分别是多少，为什么？'
    options: ['第一次显示 $75$，第二次显示 $20$；第 ② 步先读取此刻的值 $120$ 算出 $75$ 写回，第 ④ 步又把 $20$ 写进同一个名字', '第一次显示 $120$，第二次显示 $120$；读取拿到的总是最早写下的那个值', '第一次显示 $75$，第二次显示 $75$；名字被读取一次后就固定下来，后面的赋值不再起作用', '第 ② 步里同一个名字既出现在被赋的位置又出现在被读的位置，这在数学上不成立，程序会报错停止']
    answer: A
    explanation: '第 ② 步先读取此刻的 $120$，再把结果赋回，因此第一次显示 $75$；第 ④ 步重新赋值后，第二次显示读出的是新值 $20$。关键计算：$$120 - 45 = 75$$ 读取永远取“此刻”的值，所以两次显示可以不同。最易错点：把赋值里的等号当成数学上的相等，以为同一个名字不能既被读又被写。'
    difficulty: 3
    section: 练习：两个时刻
    invokes: 变量
    uses: [变量]
