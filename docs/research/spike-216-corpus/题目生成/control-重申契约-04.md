---
station: 题目生成
arm: control
variant: 重申契约
run: 4
tool_called: false
delivered: true
schema_ok: true
input_tokens: 196
output_tokens: 3650
duration_ms: 17156
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5242 字符，变体后缀=有）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

node: 变量是什么
questions:
  - id: q1
    kind: single_choice
    q: '关于变量，下列说法正确的是'
    options: ['变量是给一个会变的值起的名字，名字稳定，值随时可变', '变量名会随着值一起改变，所以每取一次值就要换一个新名字', '变量里保存的是最初写下的值，之后又写了新值也不会改变它', '同一个变量在同一时刻不能有两个取值，所以它的值一旦写下就固定不变']
    answer: A
    explanation: |
      - 为什么对：变量是「名字 + 值」的组合体，名字负责稳定地指代，值可以取不同时刻的不同取值。
      - 关键区分：$$\text{名字（稳定）}\ \neq\ \text{值（可变）}$$
      - 最易错点：把「值会变」误当成「名字也会变」——同一个名字 $\text{余额}$ 在早上对应 $50$，在晚上可以对应 $20$。
    difficulty: 1
    section: 概念：变量是什么
    invokes: 变量
  - id: q2
    kind: fill_in_blank
    q: '在 $\text{余额} = 50$ 这一步里，把值 $50$ 写进名字 $\text{余额}$ 的这个动作，叫做____（两个字）。'
    answer: ['赋值']
    explanation: |
      - 为什么对：赋值就是把某个值写进名字的那个动作，名字不变，值被写入或更新。
      - 关键一步：$$\text{余额} \leftarrow 50 \quad (\text{赋值})$$
      - 最易错点：别把它和读取混起来——读取是拿名字换回此刻的值，不写值。
    difficulty: 1
    section: 概念：赋值与读取
    invokes: 变量
  - id: q3
    kind: matching
    q: '把下面一天里发生的每个动作，与它的性质一一配对。'
    options: ['早上记下 $\text{余额} = 50$', '晚上记下 $\text{余额} = 20$', '睡前查看余额，用它换回当前的值']
    answer: ['赋值：把 $50$ 写进名字 $\text{余额}$', '赋值：用 $20$ 覆盖名字里原来的 $50$', '读取：拿名字 $\text{余额}$ 换回此刻的值 $20$']
    explanation: |
      - 为什么对：两次「$\text{余额} = $ 数字」都是在往名字里写值，属于赋值；第三次没有写值，只是拿名字换回当前值，属于读取。
      - 关键链条：$$\text{余额}=50 \;\to\; \text{余额}=20 \;\to\; \text{读取余额}=20$$
      - 最易错点：读取拿到的永远是「此刻」的值 $20$，不是第一次写下的 $50$。
    difficulty: 2
    section: 练习：两个时刻
    invokes: 变量
  - id: q4
    kind: open_question
    q: '用「余额」这同一个变量，写一段包含两次赋值和一次读取的日常记录，并说明：为什么这次读取拿不到最初写下的那个值？这段记录里哪一部分是稳定的、哪一部分是变化的？'
    answer: '评分要点：① 全程只用同一个名字 $\text{余额}$，两次「$\text{余额} = $ 数值」都是赋值，后一次会覆盖前一次的值；② 读取只是拿名字换回此刻的值，所以读到的是最后一次赋的值，而不是最初那个；③ 稳定的是名字 $\text{余额}$（始终指代同一件事），变化的是它的取值；④ 能指出若在两次赋值之间读取，会读到中间那个值，说明值随时可变。'
    explanation: |
      - 要点一：同一个名字贯穿全程，两次写值就是两次赋值，新值覆盖旧值。
      - 要点二：$$\text{写入顺序}\ 50 \to 20 \;\Rightarrow\; \text{读取}=20$$
      - 要点三：名字稳定不变，值随每次赋值而变；读取只看「此刻」，与写下它是什么时候无关。
    difficulty: 3
    section: 通用
    invokes: 变量
