---
station: 题目生成
arm: control
variant: 现状
run: 1
tool_called: false
delivered: true
schema_ok: true
input_tokens: 176
output_tokens: 7502
duration_ms: 34225
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5207 字符，变体后缀=无）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

node: 变量是什么
questions:
  - id: q1
    kind: single_choice
    q: '一天里，早上 $\text{余额} = 50$、晚上 $\text{余额} = 20$，这两个值是同一个变量「余额」在不同时刻的取值。关于「变量」，下列说法正确的是？'
    options: ['变量就是某个固定不变的数，只是换了个名字', '变量的名字和它的值会一起随时间改变，二者不分家', '变量是给一个会变的值起的名字：名字稳定，值随时可变', '同一时刻里，一个变量可以同时取好几个不同的值']
    answer: C
    explanation: '为什么对：变量是给一个会变的值起的名字，名字稳定、值可变，所以只有 C 成立。关键步骤：$$ \text{余额} = 50 \;\longrightarrow\; \text{余额} = 20 $$ 名字 $\text{余额}$ 从头到尾没变，变的只是它此刻的值。最易错点：别把「值在变」误读成「名字也在变」，名字和值是分开来看的两件事。'
    difficulty: 1
    section: '概念：变量是什么'
    invokes: 变量
    uses: []
  - id: q2
    kind: fill_in_blank
    q: '记录 $\text{余额} = 50$ 时，我们做了「把值 $50$ 写进名字 $\text{余额}$」这个动作；把某个值写进一个名字的动作，叫做____。'
    answer: ['赋值']
    explanation: '为什么对：把某个值写进名字的动作就是赋值。关键步骤：执行 $\text{余额} = 50$ 之后，变量 $\text{余额}$ 此刻的值为 $50$。最易错点：不要把它和「读取」混为一谈——读取是拿名字换回此刻的值，方向正好相反。'
    difficulty: 1
    section: '概念：赋值与读取'
    invokes: 变量
    uses: []
  - id: q3
    kind: matching
    q: '把左边的说法与右边最贴切的内容一一配对。'
    options: ['变量名', '变量值', '赋值', '读取']
    answer: ['如「余额」这样稳定的名字', '如 50、20 这类某一时刻的具体数据', '把某个值写进一个名字', '拿名字换回此刻的值']
    explanation: '为什么对：变量名是稳定的名字，变量值是某一时刻的具体数据；赋值是「写」，读取是「取」。关键步骤：$$ \text{余额} = 20 \ \text{（赋值）} \qquad \text{读余额} \Rightarrow 20 \ \text{（读取）} $$ 最易错点：读取拿到的是此刻的值，不能被当成又一次赋值。'
    difficulty: 2
    section: '概念：赋值与读取'
    invokes: 变量
    uses: []
  - id: q4
    kind: open_question
    q: '小明记录自己一天的余额：早上「$\text{余额} = 50$」，随后「读余额，得到 $50$」；傍晚「$\text{余额} = 20$」，睡前「读余额，得到 $20$」。请指出这四步里哪些是赋值、哪些是读取，写出变量 $\text{余额}$ 在每一步之后的值，并解释为什么睡前的读取拿不到 $50$。'
    answer: '评分要点：①「余额 = 50」与「余额 = 20」是赋值，分别把变量此刻的值写成 50 和 20；②两次「读余额」是读取，分别拿到此刻的值 50 和 20；③赋值会改写变量此刻的值，睡前的读取发生在「余额 = 20」之后，所以只能读到此刻的值 20，原来的 50 已被覆盖。'
    explanation: '按要点给分：能分清赋值（把值写进名字）与读取（拿名字换回此刻的值）；能说出变量在每一步之后的值；能说清读取得到的永远是此刻的值，而不是写下它时的值。'
    difficulty: 3
    section: '通用'
    invokes: 变量
    uses: []
