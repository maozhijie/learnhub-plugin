---
station: 题目生成
arm: tool
variant: 指令式
run: 5
tool_called: true
delivered: true
schema_ok: true
input_tokens: 228
output_tokens: 4128
duration_ms: 19124
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5270 字符，变体后缀=有）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

{"node": "变量是什么", "questions": [{"kind": "single_choice", "q": "关于「变量」，下列说法正确的是（　）", "options": ["变量就是一个固定不变的值，一旦确定便不能改动", "变量是给一个会变的值起的名字，名字稳定，值可以随时改变", "变量只是纸上写的一个数字，本身并没有名字", "同一个名字在同一时刻可以同时等于好几个不同的值"], "answer": "B", "explanation": "变量 = 名字 + 值：名字是稳定的，值随时间可变，所以 B 正确。\n\n同一时刻一个变量只有一个当前值，D 错；变量不是「某个固定的值」，A、C 也错。\n\n最易错点：把「变量」与「变量的值」当成同一件事。", "difficulty": 1, "section": "概念：变量是什么", "invokes": "变量"}, {"kind": "true_false", "q": "读取一个变量时，得到的总是当初给这个名字写下值那一刻的值。", "answer": "错", "explanation": "读取是拿名字换回「此刻」的值，而不是当初写入时的值，故说法错误。\n\n例如先执行 $\\text{余额} = 50$，再执行 $\\text{余额} = 20$，这时读取「余额」得到的是 20 而不是 50。\n\n最易错点：以为读取会「记住」当初写下的那个值。", "difficulty": 1, "section": "概念：赋值与读取", "invokes": "变量"}, {"kind": "fill_in_blank", "q": "把某个值写进一个名字里（例如执行 $\\text{余额} = 50$）的动作，叫作____。", "answer": ["赋值"], "explanation": "这个动作叫赋值：给名字绑定一个当前的值。\n\n写成 $\\text{余额} = 50$，就是把 50 写进名字「余额」里。\n\n最易错点：把赋值与读取混为一谈——读取是把当前值取出来，不是写进去。", "difficulty": 2, "section": "概念：赋值与读取", "invokes": "变量"}, {"kind": "open_question", "q": "小明记录账户余额的两个时刻：早上写下 $\\text{余额} = 50$，晚上又写下 $\\text{余额} = 20$。请指出这两步分别属于赋值还是读取，说明「余额」这个变量在一天中先后变成过哪些值，并解释为什么同一个变量可以有两个不同的值。", "answer": "评分要点：①两次「$\\text{余额} = \\dots$」都是赋值，即把值写进名字；②「余额」先后取过 50 元与 20 元两个值，晚上之后它的当前值是 20 元；③变量是给一个会变的值起的名字，名字固定而值可随时间改变，所以同一名字在不同时刻可以对应不同的值；④若只是查看而不重新赋值，值保持不变。", "explanation": "答题时应把「动作」与「变量」分开说：写是赋值，看是读取。\n\n关键句：名字不变、值可变，同一变量在不同时刻可以有不同取值。\n\n最易错点：认为「同一个变量」就等于「同一个值」，从而觉得两个时刻的记录互相矛盾。", "difficulty": 3, "section": "通用", "invokes": "变量"}]}
