---
station: 题目生成
arm: tool
variant: 口令式
run: 1
tool_called: true
delivered: true
schema_ok: true
input_tokens: 226
output_tokens: 4456
duration_ms: 21948
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5262 字符，变体后缀=有）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

{"node": "变量是什么", "questions": [{"kind": "single_choice", "q": "关于「变量」，下列说法正确的是", "options": ["变量是给一个会变的值起的名字；名字保持稳定，值可以随时刻改变", "变量就是一个固定不变的数，一旦写下就不再改变", "变量的名字会随着取值的改变而改变", "在同一个时刻，一个变量可以同时等于两个不同的值"], "answer": "A", "explanation": "变量是名字而不是值：名字稳定，值随时可变，所以第一项正确。\n\n把名字与值分开看：\n$$余额 = 50 \\Rightarrow 余额 = 20$$\n同一个名字在两个时刻对应两个不同的値，可见变的是值、不变的是名字。\n\n最易错点：把「变量变了」误读成「名字变了」——真正改变的是它此刻的值。", "difficulty": 1, "section": "概念：变量是什么", "invokes": "变量"}, {"kind": "matching", "q": "把左列的说法与右列对应的术语一一配对：", "options": ["把某个值写进名字", "拿名字换回当前的值", "一个会变的值所对应的稳定名字"], "answer": ["赋值", "读取", "变量"], "explanation": "赋值是「写进」，读取是「换回此刻的值」，而变量是承载它们的那个稳定名字。\n\n配对时抓住动作与对象的对应：\n$$余额 = 50 \\;\\Rightarrow\\; \\text{赋值}, \\quad \\text{读取} \\Rightarrow 余额$$\n最易错点：把「读取」记成「被写下的那一刻的值」，丢掉「此刻」二字。", "difficulty": 1, "section": "概念：赋值与读取", "invokes": "变量"}, {"kind": "true_false", "q": "早上执行 $余额 = 50$，晚上执行 $余额 = 20$；第二天程序读取 $余额$ 时，得到的值是 $20$。", "answer": "对", "explanation": "读取拿回的永远是「此刻」的值，而名字 $余额$ 在最后一次赋值时已被写进 $20$，故读到 $20$。\n\n同一名字的两个时刻：\n$$余额 = 50 \\;\\to\\; 余额 = 20$$\n最易错点：以为读到的是「最早写下」的 $50$，把写入时刻与读取结果混为一谈。", "difficulty": 2, "section": "练习：两个时刻", "invokes": "变量"}, {"kind": "open_question", "q": "用「名字—值」模型解释一个你熟悉的场景（如手机剩余电量、购物车里的商品件数）：指出哪个是变量名，哪些步骤是赋值、哪些是读取，并说明为什么某一步读取到的是此刻的值，而不是更早的值。", "answer": "评分要点：①能指出一个会变的值并给它起一个稳定的名字（如 $电量$、$件数$）；②能区分赋值（把新值写进名字）与读取（用名字换回当前值）两类动作，并各举出场景中的一处；③能说明同一名字在不同时刻可有不同取值，读取得到的永远是此刻的值，与当初写下它的时刻无关。", "explanation": "本题综合考查变量的「名字—值」模型、赋值与读取的区分。\n\n合格回答应同时给出：\n$$电量 = 80 \\;\\to\\; 电量 = 35$$\n即同一名字在不同时刻取值不同，读取只能得到此刻的值。\n\n最易错点：只描述「数字变了」，却没有说清变的是值、稳定的名字才是变量。", "difficulty": 3, "section": "通用", "invokes": "变量"}]}
