---
station: 题目生成
arm: control
variant: 现状
run: 4
tool_called: false
delivered: true
schema_ok: true
input_tokens: 176
output_tokens: 7620
duration_ms: 33543
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5207 字符，变体后缀=无）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

node: 变量是什么
questions:
  - id: q1
    kind: single_choice
    q: '关于「变量」的说法，下面哪一个是正确的？'
    options:
      - '变量的名字和值都固定不变，只是叫法不同'
      - '变量是给会变的值起的名字：名字稳定，值可以随时改变'
      - '变量之所以叫变量，是因为名字会跟着值一起改变'
      - '变量只记录一个瞬间的值，之后这个值再也不能被改写'
    answer: B
    explanation: '**为什么对**：变量的「名」稳定、「值」可变，同一个名字在不同时刻可以对应不同的值。**关键步骤**：$$\text{早上}\ \text{余额}=50,\qquad \text{晚上}\ \text{余额}=20$$同一个名字「余额」带了两个值，变的是值而不是名字。**最易错点**：把「变量」误读成名字也在变，或以为赋值一次就把值锁死了。'
    difficulty: 1
    section: 概念：变量是什么
    invokes: 变量
    uses: []
  - id: q2
    kind: multi_choice
    q: '关于「赋值」和「读取」这两个动作，下列说法正确的是（多选）。'
    options:
      - '$\text{余额}=50$ 是把值 $50$ 写进名字「余额」，这一步是赋值'
      - '读取「余额」时，拿到的永远是当初写下 $50$ 时的那个值'
      - '读取「余额」时，拿到的是这个名字当前的值，可能与上一次读到的不一样'
      - '赋值和读取是两个不同的动作，但在使用时常常成对出现'
    answer: ['A','C','D']
    explanation: '**为什么对**：赋值是「把值写进名字」，读取是「拿名字换回此刻的值」，两个动作方向相反、常成对出现。**关键步骤**：$$\text{余额}=50 \to \text{读得 }50;\qquad \text{余额}=20 \to \text{读得 }20$$同一个名字第二次读到的已经是新值。**最易错点**：以为读取会留住当初赋值时的旧值，其实读到的永远是「此刻」的值。'
    difficulty: 2
    section: 概念：赋值与读取
    invokes: 变量
    uses: []
  - id: q3
    kind: ordering
    q: '下面是同一天里围绕变量「余额」发生的四个动作，请按真实发生的先后顺序排列。'
    options:
      - '晚上读取「余额」，得到 $20$'
      - '早上给「余额」赋值 $50$'
      - '晚上给「余额」赋值 $20$'
      - '早上读取「余额」，得到 $50$'
    answer:
      - '早上给「余额」赋值 $50$'
      - '早上读取「余额」，得到 $50$'
      - '晚上给「余额」赋值 $20$'
      - '晚上读取「余额」，得到 $20$'
    explanation: '**为什么对**：读取只能拿到「此刻」的值，所以每次读取都必须排在同一时刻的赋值之后。**关键步骤**：$$\text{早上：赋值 }50 \to \text{读得 }50;\qquad \text{晚上：赋值 }20 \to \text{读得 }20$$**最易错点**：把读取排在赋值之前，或在晚上读取时仍然报出早上的 $50$。'
    difficulty: 2
    section: 练习：两个时刻
    invokes: 变量
    uses: []
  - id: q4
    kind: open_question
    q: '自己设计一个「同一个变量、两个时刻」的小情境（例如步数、电量或气温）：写出变量名，按先后顺序给出两次赋值与两次读取，并解释为什么读取同一个名字两次会得到不同的结果。'
    answer: '参考要点：①变量名与情境自洽，如「电量」；②顺序必须是先赋值后读取、再赋值再读取，如 $\text{电量}=80 \to \text{读得 }80 \to \text{电量}=35 \to \text{读得 }35$；③解释：读取拿到的永远是此刻的值，第二次读取发生在第二次赋值之后，所以得到 $35$ 而不是 $80$；④能点出「名字不变、值可变」这一变量的本质。'
    explanation: '**为什么对**：变量的名字固定，值被新的赋值覆盖，因此同一名字在不同时刻读出不同的值。**关键步骤**：$$\text{电量}=80 \to \text{读得 }80;\qquad \text{电量}=35 \to \text{读得 }35$$**最易错点**：把两次读取结果不同说成「名字自己变了」，或忘记在读之前先赋值。'
    difficulty: 3
    section: 通用
    invokes: 变量
    uses: []
