---
station: 题目生成
arm: control
variant: 重申契约
run: 2
tool_called: false
delivered: true
schema_ok: true
input_tokens: 196
output_tokens: 8097
duration_ms: 35654
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5242 字符，变体后缀=有）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

node: 变量是什么
questions:
  - id: q1
    kind: true_false
    q: '一个变量在某个时刻被赋值后，它在后面每个时刻读到的值都一定与这一次赋值相同。'
    answer: 错
    explanation: |
      **为什么错**：变量是给一个会变的值起的名字，名字稳定，但值可以随时刻改变；某次赋的值只属于「那一刻」。

      **关键步骤**：赋值是把值写进名字，之后还能再次赋值，把旧值换掉。

      **最易错点**：把一次赋值误当成「永久锁死」，忘了变量在后续时刻可以取到新值。
    difficulty: 1
    section: 概念：变量是什么
    invokes: 变量
    uses: []
  - id: q2
    kind: fill_in_blank
    q: '把 $50$ 写进名字「余额」这样一个动作，在正文里叫作 ______。'
    answer: ['赋值', '赋值操作']
    explanation: |
      **为什么对**：把某个值写进名字的动作就是赋值，这一步把 $50$ 写进了名字「余额」。

      **关键步骤**：赋值的方向是「值 $\to$ 名字」；读取的方向是「名字 $\to$ 此刻的值」，两者是一对相反动作。

      **最易错点**：把「写进去」和「读出来」混为一谈，赋值本身不是读取名字的当前值。
    difficulty: 1
    section: 概念：赋值与读取
    invokes: 变量
    uses: []
  - id: q3
    kind: ordering
    q: '下面四个动作取自一次连续记录，请按它们实际发生的先后顺序排列：'
    options:
      - '把 $20$ 赋值给「余额」'
      - '读取「余额」，得到 $50$'
      - '读取「余额」，得到 $20$'
      - '把 $50$ 赋值给「余额」'
    answer:
      - '把 $50$ 赋值给「余额」'
      - '读取「余额」，得到 $50$'
      - '把 $20$ 赋值给「余额」'
      - '读取「余额」，得到 $20$'
    explanation: |
      **为什么对**：先赋值才有值可读，所以「把 $50$ 赋值给余额」必须排在「读取余额，得到 $50$」之前；之后第二次赋值把名字改写成 $20$，再读才得到 $20$。

      **关键步骤**：赋值（写）在前、读取（拿）在后，读取只看它前面最近一次赋值。

      **最易错点**：把两次读取的结果顺序弄反，忘了读取永远只反映它那一刻的值。
    difficulty: 2
    section: 练习：两个时刻
    invokes: 变量
    uses: []
  - id: q4
    kind: single_choice
    q: '依次执行三步：① 把 $50$ 赋值给「余额」；② 把「余额」此刻的值赋给「消费」；③ 把 $20$ 赋值给「余额」。三步都做完之后，「消费」的值是多少？'
    options: ['$20$', '$30$', '$50$', '无法确定，因为「余额」后来变成了 $20$']
    answer: C
    explanation: |
      **为什么对**：第②步读取「余额」时，它此刻的值还是 $50$，所以「消费」保存的是 $50$；第③步改的是「余额」，不会回头改「消费」。

      **关键步骤**：读取拿到的永远是读取那一刻的值——第②步读到 $50$，第③步写入的 $20$ 与「消费」无关。

      **最易错点**：以为「余额」后来变成 $20$，「消费」会自动跟着更新，因而错选 $20$。

      ```svg
      <svg viewBox="0 0 340 100" xmlns="http://www.w3.org/2000/svg">
        <line x1="20" y1="62" x2="320" y2="62" stroke="#86909c" stroke-width="1"/>
        <circle cx="60" cy="62" r="4" fill="#165dff"/>
        <circle cx="165" cy="62" r="4" fill="#165dff"/>
        <circle cx="278" cy="62" r="4" fill="#165dff"/>
        <text x="30" y="48" font-size="11">t1: 余额←50</text>
        <text x="105" y="48" font-size="11">t2: 读余额得50</text>
        <text x="228" y="48" font-size="11">t3: 余额←20</text>
        <text x="52" y="84" font-size="11">消费←50</text>
        <text x="170" y="84" font-size="11">余额←20</text>
      </svg>
      ```
    difficulty: 3
    section: 通用
    invokes: 变量
    uses: []
