---
station: 题目生成
arm: tool
variant: 口令式
run: 8
tool_called: true
delivered: true
schema_ok: true
input_tokens: 226
output_tokens: 4591
duration_ms: 20992
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5262 字符，变体后缀=有）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

{"node": "变量是什么", "questions": [{"kind": "single_choice", "q": "关于「变量」，下列说法正确的是？", "options": ["变量是给一个会变的值起的名字：名字稳定，值随时可变", "变量一旦被赋值，它的名字和值都不能再变", "变量就是某个时刻的那个具体值，值一变就不再是同一个变量", "变量的名字会跟着值一起变化，值变了名字也要换"], "answer": "A", "explanation": "**为什么对**：变量的本质是「给一个会变的值起的名字」，名字稳定、值随时可变，正是 A。**关键步骤**：同一个变量 $\\text{余额}$ 早上取值为 $50$、晚上取值为 $20$，名字始终是「余额」。**最易错点**：把变量当成某个固定时刻的具体值，值一变就误以为换了个变量。", "difficulty": 1, "section": "概念：变量是什么", "invokes": "变量"}, {"kind": "true_false", "q": "读取变量时，拿到的永远是「此刻」的值，而不是当初写下它时的值。", "answer": "对", "explanation": "**为什么对**：读取就是拿名字换回当前的值，所以读到的一定是读取那一刻的值。**关键步骤**：先 $\u8868\u793a$ 处写作 $\\text{余额}=50$ 并读取得 $50$；之后 $\\text{余额}=20$，此时再读取得 $20$。**最易错点**：误以为读取会«记住»写下时的旧值，把旧值当成变量现在的值。", "difficulty": 1, "section": "概念：赋值与读取", "invokes": "变量"}, {"kind": "matching", "q": "把左边的记录／动作，与右边它真正做的事一一配对。", "options": ["$\\text{余额} = 50$", "读取 $\\text{余额}$", "$\\text{余额} = 20$"], "answer": ["把 $50$ 写进名字（赋值）", "拿名字换回此刻的值（读取）", "把 $20$ 写进名字（赋值）"], "explanation": "**为什么对**：赋值是把某个值写进名字，读取是拿名字换回此刻的值，两者成对出现。**关键步骤**：$\\text{余额}=50$ 与 $\\text{余额}=20$ 都是赋值；「读取 $\\text{余额}$」只取值、不写值。**最易错点**：把读取也当成赋值，以为读取会改变变量的值（读取只取值，不改变它）。", "difficulty": 2, "section": "练习：两个时刻", "invokes": "变量"}, {"kind": "open_question", "q": "小明一天里看到「余额」这个变量先后是 $50$ 元和 $20$ 元。请结合赋值与读取，说明这一天的记录里哪些动作属于赋值、哪些属于读取，并解释为什么同一个变量在不同时刻会有不同的值；再举出一个你自己生活中符合「名字稳定、值可变」的例子。", "answer": "参考要点：① 赋值是把值写进名字（如 $\\text{余额}=50$、$\\text{余额}=20$），读取是拿名字换回此刻的值（读取 $\\text{余额}$ 得 $50$ 或 $20$）；② 读取的结果取决于读取发生的时刻——早上读到 $50$，晚上读到 $20$；③ 变量本身没有换，名字始终是「余额」，变的只是它在不同时刻的取值；④ 例子（如气温、步数、水位、游戏分数）只要符合「名字稳定、值可变」即可。", "explanation": "**评分要点**：能区分赋值（写值）与读取（取值）；能说明读取结果随读取时刻变化而变量名字不变；能给出符合「名字稳定、值可变」的生活例子。**最易错点**：把变量说成「值固定不变的东西」，或把读取当成给变量重新赋值。", "difficulty": 3, "section": "通用", "invokes": "变量"}]}
