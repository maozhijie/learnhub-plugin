---
station: 题目生成
arm: control
variant: 重申契约
run: 1
tool_called: false
delivered: true
schema_ok: true
input_tokens: 196
output_tokens: 7295
duration_ms: 33372
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5242 字符，变体后缀=有）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

node: 变量
questions:
  - id: q1
    kind: fill_in_blank
    q: '在「$余额 = 50$」这一步中，把值 $50$ 写进名字「余额」的动作叫做 ______。'
    answer: ['赋值']
    explanation: |
      把值写进名字的动作就叫赋值：等号左边是要被写入的名字，右边是要写进去的值。

      $$余额 = 50$$

      最易错点：不要把这一步当成「余额 等于 50」的相等判断，这里的等号表示写入。
    difficulty: 1
    section: 概念：赋值与读取
    invokes: 变量
  - id: q2
    kind: single_choice
    q: '关于变量，下列说法正确的是'
    options: ['变量的名字和它此刻的值是两件事：名字稳定，值可以随时变', '变量就是把一个永远不变的数换了个名字', '同一个变量在整段过程中只能被赋值一次', '变量的名字跟着值一起变，值变了名字也要换']
    answer: A
    explanation: |
      变量是给一个会变的值起的名字，名字稳定、值随时可变，两者要分开看。

      $$余额: 50 \to 20$$

      最易错点：把「名字」和「值」当成一回事，以为值一变名字也要跟着换。
    difficulty: 1
    section: 概念：变量是什么
    invokes: 变量
  - id: q3
    kind: matching
    q: '把左边的每条记录与右边它的含义配对：'
    options: ['早上：$余额 = 50$', '中午：$余额 = 余额 - 20$', '晚上：读出 $余额$']
    answer: ['赋值：把 $50$ 写进名字「余额」，此刻变量取值 $50$', '先读取此刻的值 $50$，再写回 $30$，此刻变量取值 $30$', '读取：用名字换回当前值，读到 $30$']
    explanation: |
      三步依次对应赋值、先读后写、读取；名字不变，值随时间变化。

      $$余额 = 50 \;\Rightarrow\; 余额 = 50 - 20 = 30$$

      最易错点：把「$余额 = 余额 - 20$」当成纯赋值，忘了它必须先把此刻的值读出来再写回。
    difficulty: 2
    section: 练习：两个时刻
    invokes: 变量
  - id: q4
    kind: single_choice
    q: '某记录本上依次写着三步：① 早上写下 $余额 = 50$；② 中午写下 $余额 = 20$；③ 晚上读出 $余额$。晚上读到的是多少，为什么？'
    options:
      - '读到 $50$：变量的值由第一次赋值定下，后面的记录不算数'
      - '读到 $20$：读取拿到的是此刻的值，第二步已经把名字里的值改成了 $20$'
      - '读到 $50$：读取总是取名字最早一次写下的值'
      - '读到 $70$：两次写下的数会累加在一起'
    answer: B
    explanation: |
      变量是「会变的值」的名字：名字不变，值随时间变化，第二步的赋值把名字里当前的值改成了 $20$。

      $$余额 = 50 \;\to\; 余额 = 20$$

      因此第三步读取拿到的是此刻的值 $20$，而不是早上写下的 $50$；最易错点就是误以为第一次写下的值才算数。
    difficulty: 3
    section: 通用
    invokes: 变量
