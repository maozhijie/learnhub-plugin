---
station: 题目生成
arm: control
variant: 重申契约
run: 3
tool_called: false
delivered: true
schema_ok: true
input_tokens: 196
output_tokens: 8576
duration_ms: 37449
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5242 字符，变体后缀=有）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

node: 变量是什么
questions:
  - id: q1
    kind: single_choice
    q: '早上余额 $50$ 元、晚上 $20$ 元，同一个名字「余额」在这两个时刻表示不同数值。这最能说明变量的什么特点？'
    options: ['名字稳定，值随时可变', '名字和值都必须固定不变', '只有读取时名字才会改变', '变量只能保存一个永远不变的值']
    answer: A
    explanation: '变量是给一个会变的值起的名字：名字可以保持不变，而它当前代表的值可以随时间改变。早上与晚上读取「余额」，得到的是同一名字在不同时刻的值。最易错点是把变量名固定误解为变量值也固定。'
    difficulty: 1
    section: '概念：变量是什么'
    invokes: '变量'
    uses: []
  - id: q2
    kind: true_false
    q: '同一个变量「余额」先被赋值为 $50$，后来被赋值为 $20$；此时读取「余额」，会得到 $50$。'
    answer: '错'
    explanation: '读取变量时拿到的永远是「此刻」的值，所以「余额」已经被后一次赋值改为 $20$ 后，再读取就应得到 $20$。关键步骤是：先看到赋值 $50$，再看到赋值 $20$，最后一次赋值决定当前值。最易错点是把第一次或写下时的值当成读取结果。'
    difficulty: 2
    section: '概念：赋值与读取'
    invokes: '变量'
    uses: []
  - id: q3
    kind: fill_in_blank
    q: '在把数值 $50$ 写进名字「余额」的动作中，这个动作叫作______。'
    answer: ['赋值']
    explanation: '把某个值写进变量名字的动作就是赋值。读取则是拿名字换回当前的值，二者常成对出现。最易错点是把赋值与读取混为一谈。'
    difficulty: 2
    section: '概念：赋值与读取'
    invokes: '变量'
    uses: []
  - id: q4
    kind: open_question
    q: '请结合「变量」「赋值」「读取」说明：为什么同一个名字在不同时刻可以表示不同的值？并自己举一个生活中的例子，写出其中至少一次赋值和一次读取。'
    answer: '参考要点：变量是给会变的值起的名字；名字稳定，值可变。赋值把新值写进名字，会覆盖该名字此前保存的值；读取拿到的是读取那一刻名字当前保存的值。生活例子如「温度」：早上赋值 $18$，中午赋值 $25$，晚上读取温度得到 $25$（或按实际取值）。评分看是否能区分名字与值、赋值与读取，并说明读取取当前值。'
    explanation: '变量名像标签，值是可更换的内容；赋值改变当前值，读取取得当前值。若先赋值 $18$ 再赋值 $25$，最后读取得到 $25$。最易错点是认为读取会取最早或写下的值，而不是此刻的值。'
    difficulty: 3
    section: '通用'
    invokes: '变量'
    uses: []
