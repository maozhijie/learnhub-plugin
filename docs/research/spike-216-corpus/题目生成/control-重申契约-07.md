---
station: 题目生成
arm: control
variant: 重申契约
run: 7
tool_called: false
delivered: true
schema_ok: true
input_tokens: 196
output_tokens: 6299
duration_ms: 28434
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5242 字符，变体后缀=有）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

node: 变量是什么
questions:
  - id: q1
    kind: single_choice
    q: '关于变量，下列说法正确的是'
    options: ['变量的名字和它的值都随时可变', '变量的名字保持不变，值可以随时刻改变', '变量的值一旦确定，就不能再改变', '变量就是一个固定的数，任何时刻都相同']
    answer: B
    explanation: '变量是给一个会变的值起的名字：名字稳定、值随时可变，所以「名字保持不变、值可以随时刻改变」正确。关键一步：早上与晚上是同一个名字对应两个不同的值，$$\text{余额}: 50 \to 20$$。最易错点：把「名字不变」误当成「值不能变」。'
    difficulty: 1
    section: 概念：变量是什么
    invokes: 变量
  - id: q2
    kind: fill_in_blank
    q: '同一个名字「余额」在早上对应 50 元、晚上对应 20 元，名字始终不变而值可以改变；这种给一个会变的值起的名字，在正文中叫作____。'
    answer: ['变量']
    explanation: '这种「名字稳定、值可变」的东西就是变量，故填「变量」。关键一步：同一个名字在不同时刻取不同值，$$\text{余额}: 50 \to 20$$。最易错点：不要填成它某一时刻的具体取值（如 50）。'
    difficulty: 1
    section: 概念：变量是什么
    invokes: 变量
  - id: q3
    kind: matching
    q: '把左列的说法与右列最贴切的描述一一配对。'
    options: ['变量名', '变量值', '赋值', '读取']
    answer: ['稳定不变的那一部分', '可以随时刻改变的那一部分', '把一个值写进这个名字里', '用这个名字换回此刻的值']
    explanation: '名字稳定、值可变，所以「变量名」配「稳定不变的那一部分」，「变量值」配「可以随时刻改变的那一部分」；赋值是写入、读取是取回：$$\text{余额} = 50\ (\text{赋值}), \qquad \text{读余额} = 50\ (\text{读取})$$。最易错点：把读取当成「拿回写下它时的值」，其实读到的永远是此刻的值。'
    difficulty: 2
    section: 概念：赋值与读取
    invokes: 变量
  - id: q4
    kind: single_choice
    q: '变量「余额」的记录依次为：① 赋值为 50；② 立即读出一次，记作 $a$；③ 又读出一次，记作 $b$；④ 赋值为 20；⑤ 再读出一次，记作 $c$。则 $a$、$b$、$c$ 依次是'
    options: ['$50, 50, 20$', '$50, 20, 20$', '$50, 50, 50$', '$20, 20, 50$']
    answer: A
    explanation: '读取拿到的永远是「此刻」的值，只有赋值才会改变值：②与③之间没有赋值，两次读到的都是同一个数；④赋值之后值才变成 20。关键步骤：$$a = b = 50,\qquad c = 20$$。最易错点：以为「读一次就变一次」，或把 $a$ 当成最终值 20。'
    difficulty: 3
    section: 通用
    invokes: 变量
