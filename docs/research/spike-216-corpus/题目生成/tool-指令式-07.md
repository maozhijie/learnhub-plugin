---
station: 题目生成
arm: tool
variant: 指令式
run: 7
tool_called: true
delivered: true
schema_ok: true
input_tokens: 228
output_tokens: 2989
duration_ms: 14511
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5270 字符，变体后缀=有）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

{"node": "变量是什么", "questions": [{"kind": "single_choice", "q": "下面关于「变量」的说法，正确的是哪一项？", "options": ["变量的名字和它的值都必须不断改变", "变量是给一个会变的值起的名字：名字稳定，值随时可变", "变量就是某个时刻写下的那个数，和名字无关", "一个变量一旦被写下，它的值就固定了"], "answer": "B", "explanation": "为什么对：变量的本质是「给一个会变的值起的名字」，名字稳定、值可变，这正是 B 说的。\n关键步骤：同一名字在两个时刻可以取不同的值——早上余额是 $50$ 元，晚上余额是 $20$ 元，名字没变，值变了。\n最易错点：把变量直接等同于某个固定的数，认为值一旦写下就不再改变。", "difficulty": 1, "section": "概念：变量是什么", "invokes": "变量"}, {"kind": "true_false", "q": "判断这句话对不对：读取一个变量时，拿到的总是它最初被写下的那个值。", "answer": "错", "explanation": "为什么错：读取是拿名字换回「此刻」的值，不是换回最初写下它时的值。\n关键步骤：余额早上写 $50$ 元、晚上改成 $20$ 元，晚上去读余额得到的是 $20$，而不是最初的 $50$。\n最易错点：把「读取」当成取回历史记录，忽略了每一次赋值都会覆盖掉旧值。", "difficulty": 1, "section": "概念：赋值与读取", "invokes": "变量"}, {"kind": "numeric", "q": "变量「余额」先被赋值 $20$ 元，接着执行一步「余额 = 余额 + 30」。这一步执行完，读出的余额是多少元？", "answer": 50, "tol": 0.5, "explanation": "为什么对：等式右边的「余额」是在读取此刻的值 $20$，算出结果后再把新值写回这个名字。\n关键步骤：先读取，再计算，最后赋值\n$$余额 = 20 + 30 = 50$$\n最易错点：以为右边的「余额」不是读取（或把它当成新值），从而错算成 $20$、$30$ 或 $50+30$。", "difficulty": 2, "section": "练习：两个时刻", "invokes": "变量"}, {"kind": "open_question", "q": "一个计数器变量「计数」按顺序做三步：① 计数 = 0；② 计数 = 计数 + 1；③ 读出计数。请指出每一步属于赋值还是读取（注意有的步骤两个动作都有），并写出最后读出的数，说明理由。", "answer": ["① 是赋值：把 $0$ 写进名字「计数」。", "② 两个动作都有：右边的「计数」先被读取，取此刻的值 $0$；算出 $0+1=1$ 后再赋值回去。", "③ 是读取：读出此刻的值，所以最后读出的数是 $1$。", "理由：名字稳定、值随时可变，读取永远拿到此刻的值，赋值会覆盖旧值。"], "explanation": "为什么对：判断关键在于看每一步是「把值写进名字」还是「拿名字换回此刻的值」。\n关键步骤：②中先读后写\n$$计数 = 0 + 1 = 1$$\n再在③读出 $1$。\n最易错点：把②只当成一次赋值，忘记右边的「计数」需要先读取当前值。", "difficulty": 3, "section": "通用", "invokes": "变量"}]}
