---
station: 题目生成
arm: tool
variant: 指令式
run: 6
tool_called: true
delivered: true
schema_ok: true
input_tokens: 228
output_tokens: 4753
duration_ms: 21894
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5270 字符，变体后缀=有）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

{"node": "变量是什么", "questions": [{"kind": "single_choice", "q": "关于「变量」，下列说法正确的是？", "options": ["变量是给一个会变的值起的名字：名字稳定，值可以随时改变", "变量是一个固定不变的数，写出以后就不能再改", "变量只表示还不知道的数，和之前记录的值没有任何关系", "变量就是某一次记录下来的那个值，值一旦改变，名字也必须换掉"], "answer": "A", "explanation": "变量的核心是「名字 + 可变的值」：名字负责指认，值随时刻变化。\n\n$$ \\text{余额} = 50 \\quad \\to \\quad \\text{余额} = 20 $$\n\n最易错点：把变量当成某一个具体的数，看到值变成 $20$ 就以为变量本身换了名字。", "difficulty": 1, "section": "概念：变量是什么", "invokes": "变量"}, {"kind": "fill_in_blank", "q": "把某个值写进一个名字，例如把 $20$ 写进名字「余额」，这个动作叫做____。", "answer": ["赋值"], "explanation": "往名字里写值的动作就是赋值；与之相对，拿着名字换回它此刻的值叫做读取。\n\n$$ \\text{余额} = 20 \\quad (\\text{赋值}) $$\n\n最易错点：把赋值和「读取」混为一谈，赋值只负责写入，不负责取回值。", "difficulty": 1, "section": "概念：赋值与读取", "invokes": "变量"}, {"kind": "ordering", "q": "小明账户的余额刚开始是 $50$ 元，中午他查看了一次，晚上余额变成了 $20$ 元，睡前他又查看了一次。把下面四步按发生的先后顺序排列。", "options": ["读取余额，得到 $20$", "读取余额，得到 $50$", "把 $20$ 赋值给余额", "把 $50$ 赋值给余额"], "answer": ["把 $50$ 赋值给余额", "读取余额，得到 $50$", "把 $20$ 赋值给余额", "读取余额，得到 $20$"], "explanation": "顺序由「读到什么值」决定：先写入的值才能被随后的一次读取拿到。\n\n$$ \\text{余额}=50 \\to \\text{读取}=50 \\to \\text{余额}=20 \\to \\text{读取}=20 $$\n\n最易错点：以为「读取 $50$」可以随便排位——读取拿到的永远是此刻的值，写进 $20$ 之后就再也读不到 $50$。", "difficulty": 2, "section": "练习：两个时刻", "invokes": "变量"}, {"kind": "single_choice", "q": "某段记录依次为：\n\n$$ \\text{余额} = 50 $$\n\n$$ \\text{余额} = \\text{余额} + 20 $$\n\n执行完之后，关于「余额」这个变量的说法，哪一项正确？", "options": ["变量在两次赋值之间被重新命名了", "第二个式子右边的余额取的是此刻的值 $50$，加上 $20$ 后变量变成 $70$", "第二个式子左右两边的余额是两个不同的变量", "第二个式子执行后，变量里同时存着 $50$ 和 $70$ 两个值"], "answer": "B", "explanation": "先把右边的名字读成此刻的值 $50$，加上 $20$ 后再把结果写回同一个名字。\n\n$$ \\text{余额} = 50 \\to \\text{余额} = 50 + 20 = 70 $$\n\n最易错点：把左右两个「余额」当成两个变量，或以为一个变量能同时装两个值；名字只有一个，值只有此刻的那一个。", "difficulty": 3, "section": "通用", "invokes": "变量"}]}
