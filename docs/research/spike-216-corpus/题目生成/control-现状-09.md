---
station: 题目生成
arm: control
variant: 现状
run: 9
tool_called: false
delivered: true
schema_ok: true
input_tokens: 176
output_tokens: 11094
duration_ms: 47534
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5207 字符，变体后缀=无）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

node: 变量是什么
questions:
  - id: q1
    kind: single_choice
    q: '下面关于「变量」的说法，正确的是：'
    options: ['变量是给一个固定不变的值起的名字', '变量是给一个会变的值起的名字，名字稳定，值可以随时改变', '早上余额 $50$ 元、晚上余额 $20$ 元，说明这是两个不同的变量', '变量的名字会随着它取值的变化而一起变化']
    answer: B
    explanation: |
      **为什么对**：变量是给一个会变的值起的名字，名字稳定、值随时可变，B 正确。
      **关键步骤**：把名字与值分开看——早上余额 $50$ 元、晚上 $20$ 元，都是同一个变量在不同时刻的取值。
      **最易错点**：误以为「值变了，名字也得换」，或把两个时刻的余额当成两个变量。
    difficulty: 1
    section: 概念：变量是什么
    invokes: 变量
    uses: []
  - id: q2
    kind: multi_choice
    q: '下面关于「赋值」与「读取」的说法，正确的有（多选）：'
    options: ['赋值是把某个值写进名字，读取是拿名字换回当前的值', '赋值和读取常常成对出现：先写入、后读取，才能拿到刚写进去的值', '读取一个变量时，拿到的是写下这个值的那一刻的值，而不是此刻的值', '同一个名字可以先被写入 $50$，之后又被写入 $20$，两次写入作用在同一个变量上']
    answer: ['A', 'B', 'D']
    explanation: |
      **为什么对**：赋值是把值写进名字，读取是拿名字换回当前的值，两个动作常成对出现，故 A、B 正确。
      **关键步骤**：名字稳定而值可变，同一个名字可以先写 $50$、再写 $20$，故 D 正确。
      **最易错点**：C 把「写下它时的值」当成读取结果——读取拿到的永远是此刻的值。
    difficulty: 2
    section: 概念：赋值与读取
    invokes: 变量
    uses: []
  - id: q3
    kind: ordering
    q: '下面是围绕变量「计数」发生的一串操作，请把它们按时间先后排成正确顺序。'
    options: ['读取「计数」，拿回 $3$', '把 $0$ 写入「计数」', '读取「计数」，拿回 $0$', '把 $3$ 写入「计数」']
    answer: ['把 $0$ 写入「计数」', '读取「计数」，拿回 $0$', '把 $3$ 写入「计数」', '读取「计数」，拿回 $3$']
    explanation: |
      **为什么对**：读取拿到的永远是此刻的值，所以必须先写入，才能读出它。
      **正确顺序**：把 $0$ 写入 → 读取拿回 $0$ → 把 $3$ 写入 → 读取拿回 $3$。
      **最易错点**：把两次读取都排在两次赋值之后，那样两次都会读出 $3$，拿不回中间那个 $0$。
    difficulty: 2
    section: 练习：两个时刻
    invokes: 变量
    uses: []
  - id: q4
    kind: single_choice
    q: '变量「余额」的操作记录如下：① 把 $50$ 写入「余额」；② 读取「余额」；③ 把 $20$ 写入「余额」；④ 把 $35$ 写入「余额」；⑤ 读取「余额」。下列说法正确的是：'
    options: ['② 是赋值，③ 是读取，⑤ 读取到的值是 $20$', '①③④ 是赋值，②⑤ 是读取，⑤ 读取到的值是 $35$', '①③④ 是读取，②⑤ 是赋值，⑤ 写入的值是 $35$', '①③④ 是赋值，②⑤ 是读取，⑤ 读取到的值是 $20$']
    answer: B
    explanation: |
      **为什么对**：把值写进名字是赋值（①③④），拿名字换回当前的值是读取（②⑤）。
      **关键步骤**：⑤ 发生在最后一次赋值 ④ 之后，此刻变量里是 $35$，所以读出的就是 $35$。
      **最易错点**：D 的分类没错，却把较早写入的 $20$ 当成 ⑤ 的结果——变量只保留此刻的值。
    difficulty: 3
    section: 通用
    invokes: 变量
    uses: []
