---
station: 题目生成
arm: control
variant: 现状
run: 7
tool_called: false
delivered: true
schema_ok: true
input_tokens: 176
output_tokens: 9253
duration_ms: 41492
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5207 字符，变体后缀=无）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

node: 变量是什么
questions:
  - id: q1
    kind: single_choice
    q: '关于变量，下列说法正确的是？'
    options: ['变量是给一个会变的值起的名字，名字稳定而值可以不同', '变量就是某一时刻的固定值，名字和值都不能变', '变量只能被读取，不能被赋值', '变量在赋值后就永远保持最初的值']
    answer: A
    explanation: '为什么对：变量是给一个会变的值起的名字，名字稳定、值随时可变。关键步骤：$$早上余额=50，晚上余额=20$$。易错点：把变量当成固定值，忽略“会变”的含义。'
    difficulty: 1
    section: 概念：变量是什么
    invokes: 变量

  - id: q2
    kind: true_false
    q: '执行 $余额 = 20$ 是读取操作，因为这一步从名字“余额”中取回了当前值。'
    answer: 错
    explanation: '为什么对：$余额 = 20$ 是把 20 写进名字“余额”，所以是赋值。关键步骤：$$余额 \leftarrow 20$$。易错点：把写值当成取值，方向弄反。'
    difficulty: 1
    section: 概念：赋值与读取
    invokes: 变量

  - id: q3
    kind: matching
    q: '把左侧的两次操作与右侧“这一步在做什么、变量当前值变成多少”一一配对。'
    options: ['$余额 = 50$', '读取 $余额$（此时余额为 50）', '$余额 = 20$', '再次读取 $余额$（此时余额为 20）']
    answer: ['赋值：把 50 写进名字“余额”', '读取：得到当前值 50', '赋值：把名字“余额”的值更新为 20', '读取：得到当前值 20']
    explanation: '为什么对：赋值改变变量当前值，读取返回读取那一刻的值。关键步骤：$$余额=50 \Rightarrow 读得50，\quad 余额=20 \Rightarrow 读得20$$。易错点：读取旧值后仍以为变量保持旧值。'
    difficulty: 2
    section: 概念：赋值与读取
    invokes: 变量

  - id: q4
    kind: open_question
    q: '某程序记录余额：早上执行 $余额 = 50$；中午执行 $余额 = 余额 - 30$；下午读取 $余额$ 时显示 20。请判断每一步是赋值还是读取，说明变量余额在这些步骤中的变化，并解释为什么下午读到的不是 50。'
    answer: '早上 $余额 = 50$ 是赋值，把 50 写进名字“余额”；中午 $余额 = 余额 - 30$ 先读取当前值 50，再赋值得到 20，因此这一步同时含读取和赋值；下午读取 $余额$ 得到 20，不是 50，因为读取只取此刻的值，而变量已在中午被更新为 20。'
    explanation: '为什么对：开放题要点齐全即可。关键步骤：$$余额 50 \rightarrow 20$$。易错点：把读取到的 50 当作变量永远的值，忽略中间赋值会改变当前值。'
    difficulty: 3
    section: 通用
    invokes: 变量
