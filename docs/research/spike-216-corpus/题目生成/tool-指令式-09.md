---
station: 题目生成
arm: tool
variant: 指令式
run: 9
tool_called: true
delivered: true
schema_ok: true
input_tokens: 228
output_tokens: 4003
duration_ms: 18764
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5270 字符，变体后缀=有）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

{"node": "变量是什么", "questions": [{"kind": "single_choice", "q": '关于「变量」，下列说法正确的是？', "options": ['变量是给一个会变的值起的名字', '变量是一个写下来就永远固定的数', '变量的名字会随着值的变化一起改变', '变量一旦写入某个值，之后就不能再改了'], "answer": "A", "explanation": '变量就是「名字 + 会变的值」，名字稳定，值随时可变，所以 A 正确。\n\n以余额为例：早上是 $50$、晚上是 $20$，这两个数都是同一个变量在不同时刻的取值。\n\n- 最易错点：把「值会变」误读成「名字也会变」（C），或以为变量写入一次就固定（D）。', "difficulty": 1, "section": "概念：变量是什么", "invokes": "变量"}, {"kind": "matching", "q": '把左边对「余额」这个变量的各种说法，与右边对应的概念或含义配对。', "options": ['把 $50$ 写进「余额」这个名字', '读「余额」这个名字，取回它此刻的值', '余额早上是 $50$ 元、晚上是 $20$ 元', '「余额」这个名字在一整天里始终没变'], "answer": ['赋值', '读取', '同一个变量的不同时刻取值', '稳定的名字'], "explanation": '赋值是「把值写进名字」，读取是「拿名字换回此刻的值」，两者常成对出现，前两项因此分别对应赋值与读取。\n\n同一个名字可以有不同时刻的取值（$50$、$20$），而名字本身保持稳定。\n\n- 最易错点：把「读取」当成又写了一次值——读取只取值，不改变变量当前的值。', "difficulty": 1, "section": "概念：赋值与读取", "invokes": "变量"}, {"kind": "numeric", "q": '一份记录里有两个时刻：第一个时刻把 $50$ 写进「余额」，第二个时刻又把 $20$ 写进「余额」。在第二个时刻之后读取「余额」，读到的是多少元？只填数值。', "options": [], "answer": 20, "tol": 0.01, "explanation": '读取拿到的永远是「此刻」的值：第二次赋值把 $20$ 写进「余额」，覆盖了原先的 $50$。\n\n$$余额：\\ 50 \\rightarrow 20$$\n\n此刻读取，得到 $20$ 元。\n\n- 最易错点：以为读到的还是最早写下的 $50$，或把两次写入的值当成要相加的量。', "difficulty": 2, "section": "练习：两个时刻", "invokes": "变量"}, {"kind": "true_false", "q": '有人说：「「余额」这个名字早上代表 $50$、晚上代表 $20$，可见变量的名字也会跟着值一起改变。」这句话对吗？', "options": [], "answer": "错", "explanation": '错。变量变化的是值，不是名字。\n\n$$\n\\text{名字} = \\text{余额} \\quad (\\text{不变}) \\qquad \\text{取值}: 50 \\rightarrow 20 \\quad (\\text{可变})\n$$\n\n同一个名字「余额」在不同时刻对应不同的数，名字始终没换，换的只是它此刻的值。\n\n- 最易错点：把「名字随值一起变」当成变量的特征，混淆了「名字稳定」与「值可变」这两件事。', "difficulty": 3, "section": "概念：赋值与读取", "invokes": "变量"}]}
