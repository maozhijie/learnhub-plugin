---
station: 题目生成
arm: control
variant: 现状
run: 6
tool_called: false
delivered: true
schema_ok: true
input_tokens: 176
output_tokens: 7250
duration_ms: 30396
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5207 字符，变体后缀=无）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

node: 变量是什么
questions:
  - id: q1
    kind: single_choice
    q: '关于变量，下列说法正确的是？'
    options: ['变量是某个固定的值，一旦写下就不会再改变', '变量是给一个会变的值起的名字，同一个名字在不同时刻可以有不同的值', '变量只能存放数字，存放别的东西的名字就不算变量', '变量的名字会跟着值一起变，值一改名字就得换一个']
    answer: B
    explanation: '**为什么对**：变量是给一个会变的值起的名字，名字稳定、值随时可变，所以同一个名字在不同时刻可以取不同的值。**关键步骤**：把「名字」和「值」分开看——名字负责标识，值只回答此刻是多少。**最易错点**：把变量当成某个固定的值，以为写下 50 之后它就永远是 50。'
    difficulty: 1
    section: 概念：变量是什么
    invokes: 变量
  - id: q2
    kind: multi_choice
    q: '小明的账户余额用变量 $\text{余额}$ 记录，此刻 $\text{余额} = 50$。下面关于赋值与读取的说法，正确的有（多选）：'
    options: ['执行 $\text{余额} = 20$ 是赋值，它把 20 写进名字 $\text{余额}$', '执行 $\text{余额} = 20$ 之后，之前那个 50 仍然是 $\text{余额}$ 此刻的值', '读取 $\text{余额}$ 得到的永远是此刻的值，而不是上一次写下它时的值', '$\text{余额}$ 先后被赋过 50 和 20，所以这两个值会同时存放在 $\text{余额}$ 里']
    answer: ['A', 'C']
    explanation: '**为什么对**：$\text{余额} = 20$ 是把 20 写进名字的赋值动作；而读取拿到的永远是此刻的值，所以重写之后读到的就是 20。**关键步骤**：赋值让名字指向新值，读取取回名字当前指向的那个值。**最易错点**：以为变量会把历史值一起存着（B、D 正是这种错觉），或以为读到的值不随后续赋值改变。'
    difficulty: 2
    section: 概念：赋值与读取
    invokes: 变量
  - id: q3
    kind: numeric
    q: '小明的账户余额用变量 $\text{余额}$ 记录。早上他给 $\text{余额}$ 赋值 50；中午执行 $\text{余额} = \text{余额} - 30$；晚上执行 $\text{余额} = \text{余额} + 10$。晚上这一步执行完之后，读取 $\text{余额}$ 得到的值是多少？（只填数值）'
    answer: 30
    tol: 0.01
    explanation: '**为什么对**：每一条 $\text{余额} = \text{余额} - 30$ 都要先读取此刻的值，再把结果写回去，所以后一步用的是上一步留下的值。**关键步骤**：$$\text{余额}=50-30=20,\qquad \text{余额}=20+10=30$$ **最易错点**：拿早上的 50 直接加 10 得 60，忽略了中午那次赋值已经把 $\text{余额}$ 改成了 20。'
    difficulty: 2
    section: 练习：两个时刻
    invokes: 变量
  - id: q4
    kind: open_question
    q: '小美记录一周零花钱：先给变量 $\text{零花钱}$ 赋值 100；写作业挣到 20 元后执行 $\text{零花钱} = \text{零花钱} + 20$；买书花掉 45 元后执行 $\text{零花钱} = \text{零花钱} - 45$。请回答：（1）这段过程里哪些是赋值、哪些是读取？（2）每一步之后 $\text{零花钱}$ 变成了多少？（3）如果她想过一会儿再查看「写作业之后、买书之前」的那个余额，还能靠读取 $\text{零花钱}$ 得到吗？为什么？'
    answer: '（1）赋值共三次：$\text{零花钱} = 100$、$\text{零花钱} = \text{零花钱} + 20$、$\text{零花钱} = \text{零花钱} - 45$；读取共两次，即后两式等号右边的 $\text{零花钱}$。 （2）依次是 100、120、75。 （3）不能。读取只会给出此刻的值 75，之前的 120 已被后面的赋值覆盖，变量中并不保留历史值。'
    explanation: '**为什么对**：赋值是往名字里写值、读取是拿名字换回此刻的值，同一个变量在不同时刻可以有不同取值。**关键步骤**：$$\text{零花钱}=100 \;\to\; 100+20=120 \;\to\; 120-45=75$$ **最易错点**：把（3）答成「能读到 120」——那是把变量当成能同时保存所有历史值的仓库；要留住那一刻的值，必须在当时就读出来另存。'
    difficulty: 3
    section: 通用
    invokes: 变量
