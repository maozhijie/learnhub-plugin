---
station: 题目生成
arm: control
variant: 重申契约
run: 6
tool_called: false
delivered: true
schema_ok: true
input_tokens: 196
output_tokens: 4077
duration_ms: 17749
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5242 字符，变体后缀=有）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

node: 变量是什么
questions:
  - id: q1
    kind: single_choice
    q: '关于「变量」，下列说法正确的是：'
    options: ['变量的名字和它的值都会随程序运行不断改变', '变量的名字固定不变，它对应的值可以随时刻不同而改变', '变量代表一个永远不变的量，所以才叫“变量”', '同一个变量在不同时刻读到的值一定相同']
    answer: B
    explanation: '为什么对：变量是给一个会变的值起的名字，名字稳定、值随时可变，B 正是把名字与值分开看。关键：$$\text{名字（固定）}\ \longleftrightarrow\ \text{值（可变）}$$ 最易错点：把「名字也会变」或「值永远不变」当成变量的特征，二者都颠倒了名字与值的角色。'
    difficulty: 1
    section: 概念：变量是什么
    invokes: 变量
  - id: q2
    kind: true_false
    q: '读取变量时，拿到的是当初写下这个值那一刻的值，而不是此刻的值。'
    answer: 错
    explanation: '为什么错：读取是拿名字换回「此刻」的值，而不是翻出写下它时的历史值。关键步骤：同一个名字先后被写入不同的值后，再读取得到的是最后一次写入的值。最易错点：把读取当成“查看写入记录”，忽略了值是可变的。'
    difficulty: 2
    section: 概念：赋值与读取
    invokes: 变量
  - id: q3
    kind: ordering
    q: '一天里对变量 $\text{余额}$ 的操作如下（已打乱），请按时间先后的正确顺序排列。'
    options: ['把 20 写进 $\text{余额}$（赋值），此时 $\text{余额}=20$', '读取 $\text{余额}$，显示 50', '把 50 写进 $\text{余额}$（赋值），此时 $\text{余额}=50$', '读取 $\text{余额}$，显示 20']
    answer: ['把 50 写进 $\text{余额}$（赋值），此时 $\text{余额}=50$', '读取 $\text{余额}$，显示 50', '把 20 写进 $\text{余额}$（赋值），此时 $\text{余额}=20$', '读取 $\text{余额}$，显示 20']
    explanation: '为什么对：赋值是把值写进名字，读取是拿名字换回当前值，两个动作常成对出现——先写入 50 才能读出 50。关键：$$\text{余额}=50 \Rightarrow \text{读出 } 50;\qquad \text{余额}=20 \Rightarrow \text{读出 } 20$$ 最易错点：把两次读取都当成 50，忘了读取拿到的永远是此刻的值。'
    difficulty: 2
    section: 练习：两个时刻
    invokes: 变量
  - id: q4
    kind: open_question
    q: '综合应用：请设计一段「一天里零花钱余额变化」的记录，至少包含 3 次操作，其中既有赋值也有读取。要求：(1) 指出每一步是赋值还是读取；(2) 说明每一步操作后该变量的值是多少；(3) 解释为什么同一个名字在不同时刻能对应不同的值，而读取时不会拿到“过时”的值。'
    answer: '评分要点：(1) 能正确区分赋值（把值写进名字）与读取（用名字换回当前值），每一步判断无误；(2) 能写出同一变量名在不同时刻的不同取值，并说明读取得到的是此刻的值（例如先写入 50、花掉 30 后再读得 20）；(3) 能点出「名字固定、值可变」是变量的本质，读取永远对应最后一次写入的结果。'
    explanation: '解析要点：变量是名字与值的组合，名字稳定、值可变。关键：$$\text{余额}=50 \xrightarrow{\ \text{花掉 30}\ } \text{余额}=20$$ 此后读取只能得到 20，因为读取取的是此刻的值。最易错点：把历史值当成当前值，或以为名字本身会随值改变。'
    difficulty: 3
    section: 通用
    invokes: 变量
