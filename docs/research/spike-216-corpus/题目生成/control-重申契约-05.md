---
station: 题目生成
arm: control
variant: 重申契约
run: 5
tool_called: false
delivered: true
schema_ok: true
input_tokens: 196
output_tokens: 7083
duration_ms: 31796
---

## 提示词（生产拼装 + 变体后缀）

（每格每轮提示词同构：站模板 + 上下文包；逐轮实测 5242 字符，变体后缀=有）

## 原始应答（含修复轮，逐轮）

### 第 1 轮

node: 变量是什么
questions:
  - id: q1
    kind: single_choice
    q: '关于变量，下列说法正确的是？'
    options: ['变量的名字和值都不能改变', '变量是给一个会变的值起的名字，名字稳定，值可变', '变量是一个永远不变的数', '变量一旦赋值，读取时永远得到第一次赋的值']
    answer: B
    explanation: '正确项直接对应定义：变量是名字，值可随时刻改变。关键步骤可看成 $$\text{余额}=50 \to \text{余额}=20$$，同一个名字先后拿到不同值。最易错点是把变量名当成固定值，或以为赋值后值就锁死。'
    difficulty: 1
    section: '概念：变量是什么'
    invokes: 变量
  - id: q2
    kind: true_false
    q: '判断：先执行 $\text{余额}=50$，再执行 $\text{余额}=20$；第二次操作是读取，且变量当前值为 $20$。'
    answer: 错
    explanation: '第二次操作把新值 $20$ 写进名字，是赋值而不是读取；读取是拿名字换回当前值。执行后变量当前值是 $20$。最易错点是把“写入新值”误认成“读取到新值”。'
    difficulty: 1
    section: '概念：赋值与读取'
    invokes: 变量
  - id: q3
    kind: ordering
    q: '按时间先后排列“早上把 $50$ 元记到余额、读取一次余额；晚上把余额改成 $20$ 元、再读取一次余额”的操作顺序。'
    options: ['读取余额，得到 $20$', '赋值：$\text{余额}=50$', '读取余额，得到 $50$', '赋值：$\text{余额}=20$']
    answer: ['赋值：$\text{余额}=50$', '读取余额，得到 $50$', '赋值：$\text{余额}=20$', '读取余额，得到 $20$']
    explanation: '先有赋值把值写进名字，才有读取取回当前值；晚上要先改值再读取。关键顺序可记为：$$\text{赋值 }50 \to \text{读取 }50 \to \text{赋值 }20 \to \text{读取 }20$$。最易错点是把读取放在赋值之前，导致读到旧值。'
    difficulty: 2
    section: '练习：两个时刻'
    invokes: 变量
  - id: q4
    kind: open_question
    q: '综合题：设计一个记录一天零花钱变化的方案，要求至少写出两次赋值和两次读取，并解释为什么同一个变量在不同时刻读到的值可能不同。'
    answer: '参考要点：方案中先给变量赋值（如 $\text{零花钱}=50$），再读取得到当前值；中途可再赋值改变数值（如 $\text{零花钱}=20$），随后再读取会得到新值。变量名稳定，值随时可变；读取拿到的是“此刻”的值，而不是第一次写下它时的值。能区分赋值（写值）与读取（取值）即可。'
    explanation: '开放题按要点评分：有两次赋值、两次读取且顺序合理；能说明变量名与值分离、读取取当前值；能区分赋值与读取。最易错点是把赋值当成读取，或认为读取永远返回第一次赋的值。'
    difficulty: 3
    section: '通用'
    invokes: 变量
