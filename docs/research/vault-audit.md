# Vault 实证审计(2026-09-07)

数据源:`C:\Users\test\Desktop\my\Obsidian Vault\学习中心`(课程"数学:从数学零基础到能够支持高级软件与游戏开发",280 节点)及其 `state\`(journal/snapshots/生成任务/提示词快照)。只读审计。

## 1. 核心限制:内容样本几乎为零

- 当前课程 280 个节点文件全部为"内容待生成"占位,无一节 `content.sections`;题库目录尚无文件。
- 历史生成发生在前两轮课程身份(MathForGames→MathToDev,09-03 至 09-07),课程已重造三轮,产物随重置丢失。
- 可用实证:`state\生成任务.json` 2 条 + `state\journal.jsonl` 73 行 + 旧图快照。**节段数/字数/题型的分布统计做不了,只能给个案+图层数据。**

## 2. est 可信度:几乎没有区分度(重磅)

当前图 280 节点 est 直方图:

| est | 10 | 15 | 20 | 25 | 30 | 35 |
|---|---|---|---|---|---|---|
| 节点数 | 3 | 34 | 129 | 95 | 18 | 1 |

- p25=20、中位=20、p75=25:80% 的节点挤在 20–25 三个值。est 与图深度相关仅 0.365,与 pre 数 0.278,与 difficulty 0.520。
- 同难度内 est 几乎无离散:难度 3(116 个)est 范围 15–30、中位 20;难度 4(101 个)15–30、中位 25。
- 结论:**est 在实践中是"档位感"常数而非度量**。以 est 为主信号做复杂度分层前,必须先修 est 本身(图生成 prompt 的 est 标注纪律),或改用 difficulty(1–5,分布 11/43/116/101/9)+深度做主信号。
- 旧图 MathToDev(184 节点)同样压缩:15–30 占 96%。

## 3. 膨胀与弹性的个案证据

| 节点 | est | difficulty | 大纲节段数 | 结果 |
|---|---|---|---|---|
| 用概率量化事件发生的可能性(MathToDev) | 20 | 2 | **8** | 第 1 节段门禁失败×2 轮,任务 failed |
| 区分向量的大小方向并识别向量相等与共线(MathForGames) | 15 | 2 | **6** | 全部落盘成功 |
| 理解浮点数表示与精度误差来源(MathForGames) | 25 | 3 | ≥5(journal 可见尾节) | done,自动出题 6 道 |

- 20 分钟入门节点规划 8 节段 ≈ 每节段 2.5 分钟,还要"每节段一个可视化+练习节"——简单节点被撑复杂的直接证据。
- 生成速度基线:向量节点 6 节段共约 2 分钟(逐节 15–25 秒/次调用),即**门禁通过时生成很快;时间都耗在重试与失败上**。

## 4. 重试/失败实锤

`生成任务.json` 仅 2 条,失败 1 条,其质检报告完整复现 §2.2 诊断:

```
✗ ```plot 第 1 块不是合法 JSON 对象   ← 格式级,程序可容错
✗ 节内出现 ### 子标题                 ← 格式级,但语义上"拆节"也合理
⚠ 正文偏长(约 1936 字)…建议压缩或拆节  ← 150字纪律与"8节段规划"互相拉扯的产物
```

- 注意:该节点**大纲重排了两次**(journal 16:46、16:47 两条"节清单 8 节落盘"),说明用户/系统对膨胀大纲有过干预,仍 8 节。
- 出题侧唯一成功样本:6 道/节点(2 内容节×2+综合 3 有出入,可能是 3 内容节:2+2+2 与综合 3 的组合被截断或口径不同)——样本太少,不深究。

## 5. 其他图层数据(供分层档位设计参考)

- difficulty 分布 1–5:11/43/116/101/9;bloom:理解 87、应用 170、记忆 1、分析 22。
- 最大深度 20,叶子 134,enc 边 **0**(enc 边尚未被任何课程使用,上下文包的 enc 区块目前恒空)。
- 健康分 86,WARN 68 条(多为 R1 浅叶子)。
- practice 节点:当前课程 0 个(快照无 type 字段);交互件规范在真实数据中暂无 exercising。

## 6. 对报告的直接结论

1. **"弹性"的第一杠杆不在内容 prompt,而在图层**:est 失真+难度集中,复杂度信号必须换源或重建。
2. 膨胀个案(20min→8节)证实"通常 3–8 节"的锚定在 flash 档会取上限;下限锚定也应按 est/difficulty 传导。
3. 格式级门禁失败(plot JSON、###)已真实卡死整门任务,程序性容错+局部修复的方向得到实证支持。
4. 生成耗时本身不高(节段级 15–25s),优化重点是**首过率**而非单次速度。
